# Over-engineering Audit Report (Ponytail)

**Date:** 2026-07-16
**Status:** Analysis Complete
**Goal:** Identify and remove unnecessary complexity, boilerplate, and speculative abstractions.

## Findings

| Tag | Finding | Replacement | Path |
| :--- | :--- | :--- | :--- |
| `shrink` | 9 individual setters in `useThemeStore` | `updateTheme(updates: Partial<STTThemeState>)` | `src/stores/theme.store.ts` |
| `shrink` | Local `scales` map in `setLogoSize` | Static constant outside the store | `src/stores/theme.store.ts` |
| `shrink` | `displayRows` definition inside render | Move outside component or memoize | `src/components/SttMain.tsx` |
| `shrink` | 9 repetitive `setProperty` calls | Loop over a mapping of store keys to CSS variables | `src/components/SttMain.tsx` |
| `yagni` | `onStop` callback stored in Zustand | Use direct callback or Ref to avoid store pollution | `src/stores/stt.store.ts` |

## Summary
- **Net lines possible:** ~-40 lines.
- **Impact:** Reduced boilerplate in state management and cleaner rendering logic in the main component.

---
*Generated by ponytail-audit*

# ── Base URL (routing prefix) ──
VITE_BASE_URL=/wlkies
VITE_CORRECTIONS_URL=${VITE_BASE_URL}/api/corrections

# ── Backend server (dev proxy target) ──
VITE_SERVER_HOST=48.2.40.30
VITE_SERVER_PORT=8900

# ── WebSocket server absolute URL ──
VITE_WS_URL=ws://48.2.40.30:8900

# ── App metadata ──
VITE_APP_VERSION=1.0.0

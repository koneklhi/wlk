auto-install-peers=false
strict-peer-dependencies=true
# registry 는 커밋하지 않는다 — 개발 PC는 공개 npm, 배포 PC는 dist만 받으므로 설치 자체를 하지 않는다.
# 사내 Nexus 에서 설치해야 하면 로컬에서만 지정할 것:
#   pnpm config set registry http://48.2.1.46:20000/repository/npm-public

# WhisperLiveKit 기반 실시간 STT 통역 시스템 — 요구사항 정리

## 0. 목적

기존 whisperlive 라이브러리 기반으로 개발했던 실시간 STT 통역 체계를, whisperlivekit 라이브러리 기반으로 새로 개발한다. 본 문서는 클로드 코드를 통한 개발 진행 전 단계에서 요구사항만 정리한 것이며, 구체적인 해결 방법·구현 방식·코드 설계는 추후 클로드 코드와 함께 결정한다.


## 1. 환경

### 1.1 개발 / 테스트 환경

인터넷 연결이 가능한 Windows PC, RTX 3080 GPU. VS Code IDE에서 클로드 코드를 통해 개발과 테스트를 진행한다. 이 환경에서는 실제 마이크에 음성을 입력하는 방식이 아니라, PC 내에 저장된 mp3 또는 wav 등 음성·영상 파일을 재생시켜 그 출력을 실시간 STT로 받아 전사가 잘 동작하는지 확인하는 방식으로 테스트한다. 우선 터미널에서 번역을 제외한 실시간 STT 전사가 잘 동작하는지 확인하고, 그 다음 기존 whisperlive에서 사용하던 React 기반 웹 UI를 그대로 연결해 번역과 UI 표출까지 잘 동작하는지 확인한다.

### 1.2 배포 환경

외부 인터넷이 차단된 폐쇄망 Windows PC, RTX 5090 GPU. 개발 PC에서 검증이 끝난 코드를 옮겨 배포 테스트를 진행한다. 이 환경에서는 실제 마이크에 음성을 입력하며 테스트한다. 폐쇄망이므로 모든 모델·라이브러리는 오프라인 / 로컬 경로 기반으로 동작해야 한다.

### 1.3 모델

전사(STT) 모델은 whisper-large-v3-turbo를 사용한다. 번역 모델은 기존 whisperlive에서 사용하던 OSS 20B LLM을 그대로 사용한다.


## 2. 음성 입력 및 ASR 처리

### 2.1 마이크 캡처 위치

React 측 코드를 추가로 수정하기 어려운 상황이므로, 마이크 캡처는 기존 whisperlive 시스템과 동일하게 ASR 백엔드 측에서 수행한다.

### 2.2 한국어/영어 전용 강제

한국어와 영어 두 언어만 들어오는 상황을 강제한다. 한국어와 영어 음성 인식률을 극대화하는 것이 목표이며, 특히 한국어와 영어가 한 발화 안에 섞이는 Code-Switching 상황에서 단어 유실, 환각, 문장 조기 확정 등의 문제가 발생하지 않도록 처리해야 한다.


## 3. 문장 단위 출력

### 3.1 한 문장씩 깔끔하게 출력

음성이 입력되면 텍스트로 전사되어 UI 상에서 한 문장씩 깔끔하게 출력되어야 한다. 한 문장이 끝나면 다음 문장은 줄바꿈되어 새로운 단락에서 시작된다.

### 3.2 비확정 / 확정 플래그

문장이 아직 확정되지 않고 입력을 받고 있는 중이면 비확정 상태임을 나타내는 플래그를 함께 보내, UI 상에서는 옅은 회색으로 문장이 생성되는 과정이 보이도록 한다. 문장이 확정되면 확정 플래그를 보내 UI에서 일반 색상으로 고정 표시되도록 한다.

### 3.3 문장 단위 확정 로직 — 새로 정한다

기존 whisperlive 환경에서는 문장 단위로 깔끔하게 출력되도록 만들기 위해 여러 임시방편 기법(예: 같은 문장이 10번 반복되면 확정, 타임스탬프 변화량이 임계치 이하이면 확정 등)을 추가했었다. 이 기법들은 whisperlive 환경에서 동작시키기 위한 것이었고, whisperlivekit으로 옮길 때 그대로 이식할 필요는 없다. 새 시스템에서 문장 단위 확정 로직은 클로드 코드를 통해 적합한 아이디어들을 제공받고 검토하여 결정한다.


## 4. 번역

### 4.1 번역 트리거

문장이 확정되면 그 시점까지 들어온 문장을 대상으로 번역을 수행하고, 번역 결과를 UI에 출력한다.

### 4.2 번역 파이프라인

번역에 사용하는 OSS LLM 모델, 프롬프트, 번역기 모듈을 포함한 파이프라인은 기존 whisperlive에서 구현된 구조를 그대로 사용한다.


## 5. 필터링 및 단어 교정

### 5.1 환각 제거 및 단어 대치

음성이 텍스트로 전사되면 환각으로 생성된 문장이나 단어가 있는지 검사하여 제거하고, 사전에 정의한 대치 단어 리스트에 포함된 단어가 있는지 검사하여 해당 단어를 대치한다. 이 로직은 기존 whisperlive에서 구현된 `_filtering____init__.py`와 `manager.py`의 내용을 그대로 새 시스템에 적용한다.

### 5.2 단어장 / Glossary 동적 관리

사용자가 운용 중에 단어 교정 사전과 번역 glossary를 동적으로 추가하거나 삭제할 수 있는 기능을 제공한다. 이 기능은 기존 whisperlive에서 적용했던 구조와 인터페이스를 그대로 사용한다.

### 5.3 사전 갱신 즉시 반영

단어 교정 사전 또는 번역 glossary가 갱신되면 그 변경사항이 즉시 시스템에 반영되어 다음 전사 / 번역부터 새 사전이 적용되어야 한다.


## 6. 시스템 운영

### 6.1 React UI와의 호환

기존 whisperlive에서 사용하던 React 웹 UI를 그대로 재사용한다. React 측 코드 수정은 어려운 상황이므로, 기존에 구현된 기능 외에 추가로 필요한 기능은 가능한 한 백엔드 측에서 구현한다.


## 7. 추후 결정 항목

다음 항목들은 본격적으로 whisperlivekit 환경에서 개발을 진행할 때 결정한다. 본 요구사항 정리 단계에서는 결론을 내지 않는다.

- 문장 단위 확정 로직의 구체적인 알고리즘

# 실험 로그 (STT WER/F1 개선) — STATE (요약·현행상태)

이 파일은 **항상 읽는 요약본**이다. 현행 측정 regime · 베이스라인 · 이월 핵심사실 · 빠른참조 · **코드 세대(epoch) 게이트**만 담는다.
개별 실험의 전체 서술은 [EXPERIMENTS_LOG.md](EXPERIMENTS_LOG.md)에, Exp-001~130은 [PHASE2_EXPERIMENTS.md](PHASE2_EXPERIMENTS.md)에 있다.
측정·채택 규율은 [CLAUDE.md](CLAUDE.md) §4·§3.8을 따른다. 기록은 `/log-experiment`로 수행한다.

> **3계층 구조**
> - **이 파일 (STATE)** — 항상 읽음. 요약·regime·baseline·이월핵심·빠른참조·epoch 게이트. (~150줄 상한)
> - [EXPERIMENTS_LOG.md](EXPERIMENTS_LOG.md) **(LOG)** — Exp-131~ 전체 서술. **온디맨드**: 특정 Exp 상세는 `grep "Exp-NNN"`으로 해당 블록만.
> - [PHASE2_EXPERIMENTS.md](PHASE2_EXPERIMENTS.md) **(ARCHIVE)** — Exp-001~130, 동결. 아주 오래된 결론 추적 시에만.

---

## 읽기 규약 (Claude — 매 개선 세션)

1. 세션 시작 시 **이 STATE 파일만** 읽는다(짧음). LOG/ARCHIVE를 통째로 읽지 않는다.
2. 특정 과거 실험 상세가 필요하면 → [EXPERIMENTS_LOG.md](EXPERIMENTS_LOG.md)에서 `grep "Exp-NNN"`으로 해당 블록만.
3. 과거 결론을 현재 작업의 채택/기각 근거로 인용하기 전 **아래 epoch 게이트**를 반드시 적용한다.

## 코드 세대 (Epoch) — stale 결론 차단 게이트

> **왜**: 실패 모드를 바꾸는 구조적 코드 변경 전후로 디코더 파라미터의 트레이드오프가 달라진다.
> 다른 세대에서 나온 결론을 현재 코드의 **확정 근거**로 쓰면 오판한다(예: 초기 파라미터 튜닝 ↔ 언어고정·비음성억제 도입 후).

> ⚠️ **기질(substrate) 전환 — 2026-07-05 소급 확인 (Exp-158)**: **E1~E4는 전부 의도한 `whisper-large-v3-turbo`가 아니라 `base`(74M)** whisper 위에서 측정된 것이었다 — `SimulStreamingASR.__init__`이 `model_dir`을 무시하고 `model_path`만 확인하던 배선 버그(파라미터 차원 검증으로 확정: base=71.8M/512차원, turbo=807.0M/1280차원). 폐쇄망 배포는 인터넷이 없어 base 자동다운로드도 실패하는 **배포 블로커**였다. 아래 **E1~E4 항목의 파라미터 값·WER 수치는 전부 무효**(재검증 대상)이나, **구조적 코드 결정**(SimulStreaming·Sortformer 채택, 언어전환 프로토콜 Exp-150~153, 필터 Exp-139/152, 반복필터 Exp-002/028/057)은 코드에 그대로 남아있어 유효하다. **Epoch 5(E5) = turbo 기질, 최초로 의도한 모델 위 측정.**

- **현재 master = Epoch 6 (E6, turbo 기질 — new_speaker 동일언어 스킵) — 2026-07-22**: `fix/samelang-no-refresh` 머지(Exp-196, 커밋 `3ad14a6` = `47fd76c`+`4448288`) — 화자전환(new_speaker) eager 언어감지가 기존 언어와 동일함을 확정하면 경계 재디코딩(`refresh_segment`)을 스킵하고 화자 귀속(`token.speaker`)·`global_time_offset`만 갱신, 쿨다운으로 eager가 None이 되는 구간은 직전 감지결과(`_last_eager_lang_result`)를 캐시해 스킵 판정에만 재사용. **new_speaker의 실패모드를 바꾼다** — 종전(E5)은 화자전환마다 경계를 무조건 재디코딩해 중복방출(ytn2 "우선 우선")을 냈으나, 이제는 동일언어 전환에서 스킵해 중복은 해소하되 **오탐(진짜 다른 화자를 동일언어로 오판) 시 화자 블렌딩** 위험이 새로 생긴다 — **Exp-155(E4)가 같은 가설을 넓은 리셋 범위(리셋 블록 전체 생략)+base 기질에서 시도해 bong1 new_speaker 15/15 100% 스킵→진짜 다른 화자 블렌딩→화자F1 -4.1로 기각됐던 것과 대비**된다. 이번은 재디코딩 호출 하나만 스킵하는 좁은 범위+turbo 기질이라 그 실패 지점(리셋 전체 생략)을 피해가며, bong1 화자F1 73.7→78.8%·ytn2 90→94.7%(2단계 개선분 합산)로 개선 측정(커밋 메시지, N=1 스크리닝). **E5의 new_speaker·경계재디코딩 경로에 얹힌 결론은 `[E5·재검증]` 대상**(frame_threshold 배포레버 Exp-193·boundary_reconcile Exp-192·keep_secs Exp-171/174 등 — 스킵 신설로 그 경로들이 도달하는 빈도·조건이 달라졌다). 상세 = EXPERIMENTS_LOG.md Exp-196.
- **Epoch 5 (E5, turbo 기질 — 이력) — 2026-07-05**: 두 버그 수정 머지(Exp-158, 커밋 9e3217e/d11f8b0/415ac39) — ① `model_dir` 배선 버그 수정(SimulStreamingASR가 실제로 turbo 로드) ② `detect_current_language()`가 `@torch.no_grad()` 밖에서 실행돼 turbo 인코더(807M·32층) forward가 autograd 그래프 보존, 0.2s→31.96s(~160배)로 폭주해 실시간 stall(FFmpeg read timeout)을 일으키던 버그 수정. **E1~E4의 모든 파라미터 결론(beam·CRT·PLC·logprob·frame_threshold 등)이 base 기질 위에서 나온 것이므로 turbo 기질에서 전면 재검증 필요했다**(대부분 E5 내에서 재검증 완료 — 아래 이월 핵심사실 `[E5·재검증완료]` 항목 참조). 1차 turbo N=3: bong1 28.1%(base 대비 개선)/ytn2 41.9%(base 대비 대폭회귀)/sbs1 16.1%(개선이나 실시간 lag 최대 41s, RTX 3080). held-out 미측정. **new_speaker 경계 재디코딩이 화자전환마다 무조건 발동하는 구조였다**(E6에서 동일언어 조건부 스킵으로 변경) — new_speaker·화자전환 경로에 걸린 파라미터·구조 결론은 `[E5·재검증]` 대상.
- **Epoch 4 (E4, base 기질 — 이력, 수치 무효)**: E3 + **diar-ON 언어전환 경로 배선 활성화**(`prev_lang fallback`로 마커/2.5s 트림 실발동 + `PuncSegment.hard_boundary`로 diar 병합 경계보존). **Exp-153 머지 (dc312bb, 2026-07-03).** E3에서 **dormant**였던 전환 메커니즘이 측정경로(diar-ON)에서 처음 실동작 → 실패모드 변화: 전환경계 단어보존(§3.2/Q4) 획득 + **신규 재디코딩 filler 환각**("You know, in Bukhpil"류)·**마커 과분할**(F1 precision↓). E2 파라미터 결론(PLC·beam·CRT·nonspeech; Exp-131~149)은 전환 활성화로 거동이 또 달라지므로 **[E2·재검증]** 유지. **PLC는 Exp-154서 재평가 완료 → 기본값 4.0 채택**(전환세금 제거·배선 후 E1/E2 3회기각이 채택으로 전환; ytn2 무휴지 코드스위칭 유일경로). 단, **이 전체가 base 기질 위 측정 — 구조 결정만 유효, 파라미터 값은 재검증 대상.**
- **Epoch 3 (E3, 이력)**: E2 + **언어 전환 프로토콜 재설계**(전환 시 오디오 절단으로 재디코딩 세금 제거 · `_check_short_silence_language` SOT 배선버그 수정 · LanguageSwitch 문장경계 마커). 단계1 머지 (6db5ea1, 2026-07-02) — **단 diar-ON(측정 기본)에서 dormant**였음(Exp-153이 배선으로 활성화).
- **Epoch 2 (E2, 이력)**: SimulStreaming + diar-ON + `lang_restrict_koen=True`(후처리 CJK/주석 필터). Exp-139 머지(2026-07-01), Exp-142에서 logprob=-2.0 N=3 베이스라인 확정(bong1 37.5 / ytn2 31.5 / sbs1 19.6).
- **세대 경계 규칙**: 파라미터 값 변경(PLC·beam·frame_threshold·CRT 등)은 epoch를 **올리지 않는다**(같은 세대 내 실험). 언어고정·비음성억제·디코더 교체·VAD 파이프라인 변경 등 **실패 모드를 바꾸는 구조 변경**만 세대를 올린다.

**▶ epoch 게이트 (필수)**: 과거 Exp 결론(특히 파라미터)을 현재 작업의 채택/기각 근거로 인용하기 전, 그 Exp의 epoch가 *지금 측정 대상 코드*의 epoch와 같은지 확인한다. **다르면 확정 사실이 아니라 '방향 신호'로만** 쓰고 재검증한다.
예: Exp-131~137(E1)의 PLC·beam·frame_threshold 기각은 E2 코드(언어고정+비음성억제)에선 **재검증 대상** — 그 기각 사유인 "bong1 max 폭주(웃음→CJK 환각)"를 E2가 직접 손대기 때문.

---

## 현행 측정 regime (2026-06-30 갱신 — 2계층 스크리닝/확정)

- **세션 언어모드 매트릭스(CLAUDE.md §3.2·§3.8)**: 측정 세트는 auto/ko/en 세 언어모드로 나뉜다. `--lan`은 서버 기동당 전역 1값이라 모드가 다른 파일은 **run을 분리**해 측정한다.
  - **auto 테스트(채택/기각)**: `bong1` + `ytn2` + `sbs1` (`--lan auto`)
  - **ko 테스트(채택/기각)**: `kor1` + `kor2` + `kor3` (`--lan ko`; 한국어 단독 낭독, Exp-178에서 auto 모드 붕괴가 발견돼 편입)
  - **held-out**: auto 정량 = `ytn1`(`--lan auto`, ytn2 동일 이벤트 쌍둥이 코드스위칭), en 정량 = `eng1`(`--lan en`, 영어 회귀 감시). **정성 sanity = `kinno`**(`--lan auto`, 2화자; 정답 텍스트 부정확 → **WER/F1 게이팅 제외**, 대규모 누락/환각·거친 화자/문장 분리만).
  - **① 스크리닝(평소)**: `eval.py --repeat 1` — 빠른 방향 탐색·catastrophic 회귀 감지. 1회 수치는 방향 신호로만 해석한다.
  - **② 채택 확정(머지 직전)**: `eval.py --repeat 3` — N≥3회 **median + min/max/stdev** 함께 본다. 이 단계에서만 **fail-fast 금지**(분산 자체가 데이터).
  - **ko 모드 신 베이스라인**: kor1~3은 방금 테스트셋에 편입돼 이 표(§ 현재 베이스라인)엔 아직 미포함 — 2-F1 신 베이스라인 재측정과 함께 후속 실행 단계로 남는다. 완전한 ko 세션 거동은 `feat/session-lang-lock` 브랜치 머지 이후 확립.
- **측정 경로**: 경로 C(VBCable 루프백)만. provenance 게이트 필수 — 매 측정 첫 줄 `[provenance] code=wlk branch=master@… vbcable=ok …` 육안 확인.
- **측정 기본 설정**: 화자분할 ON (Sortformer + `--compression-ratio-threshold 3.0`). **정답 = `test_data/<name>.txt` 단일 파일 canonical**(`[spkN]` 헤더+화자·문장 전처리 완료; 2026-07-18부로 `_speak,sentence_sperate.txt` 접미사 규약 폐지·`<name>.txt`로 통합, `eval.py` 파서 동기화 완료, master 머지됨).
- **★ 측정 regime v2 (2지표 분리)**: 지표를 **화자분리 F1**(`[spk]` 전환 경계가 전사 줄분리로 실현되는지)·**문장분리 F1**(동일 화자 온점 경계)로 **분리** 측정·기록. 구 단일 F1(빈 줄 경계)과 **비교 불가**. 신 Exp는 두 F1을 따로 남긴다. 2-F1 metric 코드는 **구현 완료**(TDD, master 머지됨); **2-F1 신 베이스라인 재측정이 다음 실행 단계**. 정본 = [docs/TRANSCRIPTION_REQUIREMENTS.md](docs/TRANSCRIPTION_REQUIREMENTS.md).
- **채택 우선순위(② 단계, regime v2)**: **화자분리 F1 worst-case 미회귀 → WER max 미회귀 → WER median 개선 → 문장분리 F1**(후순위·Case A 허용). **Case B(단어 중간 분절)는 수치 무관 hard-fail.**
- **개선 1순위**: `ytn2`(짧은 텀 코드스위칭) + `bong1`(다화자 장시간) 공동 최우선. **데이터 특화 하드코딩 금지 — 개선은 일반화돼야 한다.**

## 현재 베이스라인 (Epoch 5 — turbo, 확정)

> **turbo 기질 baseline (2026-07-06, Exp-161 최신)** — PLC 기본값 None 전환(Exp-160) + `audio_max_len` 30.0→15.0 전환(Exp-161) 이후 수치가 **현재 master 기본 설정 기준**. diar-ON, CRT=3.0, **PLC=None**, **audio_max_len=15.0(기본)**, beams=2.
> **확정 게이트(max)**: bong1≤30.5% / ytn2≤34.5% / sbs1≤16.1% (Exp-161 N=3로 갱신 — 3파일 모두 개선).
> JSON: `.omc/benchmarks/eval_20260705_2338_audiomax15_N3.json`(테스트 N=3) · `eval_20260706_0002_audiomax15_heldout.json`(held-out)

| 파일 | WER median | WER max | WER min | WER stdev | F1 median(구 regime) | 측정 N |
|------|-----------|---------|---------|-----------|-----------|--------|
| bong1 | 30.5% | 30.5% | 29.3% | 0.7% | 50.0% | 3 |
| ytn2  | **28.1%** | 34.5% | 24.6% | 5.0% | 38.5% | 3 |
| sbs1  | **14.9%** | 16.1% | 13.1% | 1.5% | 16.7% | 3 |
| ytn1(held-out) | 21.5% | — | — | — | 38.1% | 1 |
| eng1(held-out) | 4.8% | — | — | — | 0.0% | 1 |

> **F1 열 = 구 regime**(단일 문장분리 F1, 정답 빈 줄=화자전환 경계). **신 regime v2의 화자분리 F1/문장분리 F1과 직접 비교 불가** — 2-F1 metric 코드는 구현 완료(master 머지됨); **2-F1 신 베이스라인 재측정이 다음 실행 단계**(kinno held-out은 정성 sanity라 이 표에 미포함). 요구사항·측정 정본 = [docs/TRANSCRIPTION_REQUIREMENTS.md](docs/TRANSCRIPTION_REQUIREMENTS.md).

**테스트 평균(median)**: WER 24.5% (Exp-160 직후 27.9%에서 대폭 개선, sbs1이 주도) · **분산 대폭 감소**(전 파일 stdev 1~5%대 — worst-case 안정성 확보, §3.8 최우선 원칙과 정합). **sbs1 실시간 lag**: 41s → **2s대**로 안정화(Exp-161).

**참고(audio_max_len=30.0 시절, PLC=None만 적용 — Exp-160, `audio_max_len` 변경으로 대체됨)**: bong1 30.5%/max33.5%/stdev3.0 · ytn2 30.0%/max38.4%/stdev7.2 · sbs1 23.2%/max30.4%/stdev10.6 · ytn1(held-out) 22.7% · eng1(held-out) 7.6%.

**참고(PLC=4.0 시절 E5 baseline, Exp-158/159 — PLC 기본값 변경으로 대체됨)**: bong1 28.1%/max34.7% · ytn2 41.9%/max47.3% · sbs1 16.1%/max31.0% · ytn1(held-out) 33.1% · eng1(held-out) 3.8%. PLC=None 대비 bong1·sbs1은 median이 더 좋았으나(단 sbs1은 고분산 파일), **ytn2는 PLC=4.0의 스퓨리어스 전환이 방송클로징 환각을 유발**해 크게 나빴다(Exp-160 규명).

**참고(base 기질 E4 baseline, 무효화됨 — Exp-153 N=3)**: bong1 36.3%/max37.5 · ytn2 25.6%/max26.1 · sbs1 20.2%/max26.8. turbo 대비 bong1·sbs1은 개선, **ytn2는 대폭 회귀**(코드스위칭 스캐폴딩이 base 거동에 맞춰 튜닝됐던 게 원인 추정 — 재조사 필요).

**✅ 실시간 lag 해결(Exp-161)**: sbs1 lag 41s(Exp-158, RTX 3080) → `audio_max_len` 30.0→15.0 전환으로 **2s대 안정화**(3회 재현: 2.04/2.03/2.17s). 배포(RTX 5090)는 더 여유 있을 것으로 예상되나 재확인 권장.

## 이월 핵심사실 (distilled — 상세는 LOG / [PHASE2_EXPERIMENTS.md])

> 태그: **[불변]** = 측정방법론·데이터특성·구조 → 모든 epoch에서 유효. **[E1·재검증]** = E1에서 측정한 파라미터 트레이드오프 → E2 코드에선 재검증 대상.
> **substrate 태그(2026-07-05 추가)**: **[모델무관·유지]** = base/turbo 어느 기질에서도 유효(구조 결정·측정방법론). **[base전용·재검증]** = base 기질(E1~E4) 위에서 나온 파라미터 결론 → turbo에서 전면 재검증 대상. **[base진단·재확인필요]** = base 기질의 실패모드 진단 → turbo에서 같은 현상인지 미확인.

- **[불변][측정][모델무관·유지]** 경로 C(VBCable)만 채택 판정 기준. 경로 A(PCM 주입)는 실사용과 무관 → 폐기.
- **[불변][측정][모델무관·유지]** Exp-106~129 전체 기각 — silent code-version trap(잘못된 cwd로 변경 미반영 측정) + VBCable 간헐 불안정 + provenance 미기록. provenance 하니스(Exp-130) master 머지 완료가 새 기준점.
- **[불변][디코더][모델무관·유지]** SimulStreaming 채택(Exp-001) — LocalAgreement는 영어 코드스위칭을 통째 누락하고 발화 후반 커버리지를 잃음. AlignAtt 실출력 토큰엔 **구두점이 없어** 구두점 기반 확정이 미발동 → 확정 신호는 VAD silence·세그먼트 경계·언어 전환에서 찾는다.
- **[E5·재검증완료][디코더][모델무관·유지]** `beam=2`(기본값)가 turbo에서도 최적으로 재확인(Exp-162) — beam=1(greedy)·beam=3 스크리닝 모두 3파일 중 2~3개가 게이트 초과(beam=3은 전부 초과). base 기질(Exp-125: beam2 28.1%→beam3 29.6%→beam4 40.4% 단조증가)과 동일 방향 — [base전용·재검증]에서 [모델무관·유지]로 격상. CRT=3.0도 재확인(2.5는 게이트 초과, 2.8은 baseline과 구분 안 됨) — 변경 불필요.
- **[E5·재검증완료][언어][모델무관·유지]** `periodic_lang_check`(PLC) 기본값 4.0(Exp-154, base 기질) → **turbo에서 N=3 재검증 결과 None(비활성)으로 전환(Exp-160)**: PLC=4.0의 주기적 재확인이 ytn2에서 스퓨리어스 언어전환을 오탐 → 트림+재디코딩 발동 → 방송클로징 환각("김정은 기자입니다" 반복)을 유발함을 정성 확인. PLC=None으로 해당 환각 완전 소멸, ytn2 median -11.9pp·max -8.9pp 개선. base 기질에서 "PLC=None이면 언어 고착 후 환각 급증"이었던 Exp-131/143/145 결론은 **turbo에서 재현되지 않음**(모델별 상이 — [base전용] 태그였던 근거가 뒤집힘). PLC=2.0(강화 방향)은 N=1에서 유사 개선 신호를 보였으나 **N=3에서 재현 실패**(ytn2 median 46.8%로 오히려 baseline보다 나쁨) — N=1 스크리닝 한계 사례로 기록.
- **[불변][diar][모델무관·유지]** Sortformer 과분할로 단일화자(sbs1) 문장분리 F1 급락(diar-ON 36.4% vs diar-OFF 76.2%, ref=3 vs hyp=9–11). **[E4·규명][base진단·재확인필요]** 이 과분할 근원은 **화자전환 이벤트가 아니라 문장경계 과분할**(`tokens_alignment` 온점분할) — Exp-155서 sbs1의 `new_speaker`(ChangeSpeaker) 발동이 **0회**로 확인(화자전환 조건부 리셋으론 sbs1 F1 개선 불가). **[E1][base전용·재검증]** ChangeSpeaker 2.0s 디바운스는 ytn2 회귀(Exp-106); nonspeech_prob=0.35는 bong1 환각↓이나 ytn2 부작용(Exp-107). **[E5·Exp-167 규명·모델무관·유지]** 이 문장경계 과분할의 근본원인 4갈래(Silence마커 순서경쟁·flush 0토큰·QG streak→refresh·구두점갭기반 (c)분기)를 실증 규명, 3갈래는 코드수정 완료(`tokens_alignment.py`)이고 나머지 하나("것으로/보입니다"류 잔존)는 버그가 아니라 **Silence 토큰 생성 문턱(`MIN_DURATION_REAL_SILENCE=0.4`) 부근의 VAD 측정 민감도** 때문임을 3/3 케이스로 실증(0.39s→병합, 0.45s→분할). 남은 sbs1 과분할은 문장확정 신호조합 정책(호흡성 pause를 하드경계로 볼지) 재검토가 필요한 **별개 사안**으로 재분류.
- **[불변][데이터특성][모델무관·유지]** **ytn2 후반 통역 구간의 `한국군 사성자… 한국군 사령관으로` / `상당한.. 상당한 진전이`는 전사 결함이 아니라 실제 화자의 비유창성**(사용자 청취 확인, 2026-07-21). 화자가 `사성자`·`상당한`의 말끝을 흐리다 구절을 다시 시작한 발화라 **전사가 오히려 정확**하다 — 개선 대상 아님. 로그 근거도 정합: 두 방출 사이에 `Refreshing segment`가 없고 절대 타임스탬프가 겹치지 않으며 0.7~0.9s 떨어져 있다(재디코딩 아티팩트라면 시각이 0.0~0.06s로 겹친다). **이 구간을 근거로 반복 억제 로직을 만들지 말 것.** 유사 사례 판단 절차는 CLAUDE.md §4 "원본 발화 확인 규칙" 참조.
- **[불변][환각][모델무관·유지]** bong1 웃음 구간에서 Whisper 환각 다발(JSON 분석 확인) — 현상 자체는 Whisper 계열 공통 특성으로 추정되나 구체적 양상은 turbo에서 재확인 필요.
- **[불변][환각·E2규명][base진단·재확인필요]** **bong1 worst-case 근본 원인 = 비음성(웃음·박수) 구간 언어 오감지** → 중국어/일본어 환각 캐스케이드(Exp-138 코드 규명). E2(lang_restrict_koen)가 CJK 언어토큰을 막아도 환각은 **사라지지 않고 라틴/한글 쓰레기로 형태만 바뀜**(Exp-139). → 비음성 구간 자체를 전사에서 배제(VAD/no_speech)하는 **Layer 3b가 미해결 1순위 과제**. turbo N=3(Exp-158) 정성분석에서도 bong1에 필러/반복 환각("Thank you" 연쇄) 확인 — 양상은 다르나 문제 자체는 잔존. **[E5·Exp-163 실측강화]** turbo 'thank you' 필러를 backend 후처리 반복필터(v1 연속 n-gram·v2 윈도우-앵커)로 억제 시도 **실패·기각** — 필터가 스톰을 올바로 탐지·refresh해도 ①디코더가 refresh 후 필러를 **재생성**해 출력 드롭으로 못 막고 ②storm-refresh가 ytn2 정렬을 교란해 max 34.5→46.8 catastrophic 회귀. **→ 필러는 출력 후처리가 아니라 반드시 원천(Layer 3b 비음성 게이팅)에서 차단해야 함이 실측 확인됨.** **[E5·Exp-164 실측강화·모델무관·유지]** Layer 3b 원천 차단을 기존 두 메커니즘으로 시도 **실패·기각**: ① no_speech SOT 게이트는 **구조적으로 무효** — `_concat_segments()`가 롤링버퍼 전체(최대 audio_max_len=15s, 이전 실제 발화 포함)를 판정해 tail에 웃음/필러가 있어도 확률이 절대 안 오름(3파일 1221샘플 전부 정확히 0.000, 어떤 임계값도 무의미). ② VAC(Silero) 임계값 상향(0.4)은 신호 자체는 살아있으나(mean0.715·min0·max0.996) **개념적으로 부적합** — 웃음·필러는 실제 음성에너지(성대진동)를 동반해 "speech"로 통과되므로 임계값을 올려도 억제 안 되고(전사에 필러 그대로 잔존) bong1 게이트만 초과(34.4>30.5). **→ Layer 3b는 세그먼트 tail만 판정하는 구조 변경 또는 별도 비-ASR 웃음 분류기가 필요 — 기존 no_speech/VAC 확장으로는 도달 불가함이 확정.** **[E5·Exp-165 실측강화·모델무관·유지] no_speech tail 판정도 실패 → 폐기 확정**: Exp-164의 다음가설(tail만 판정하면 오르나?)을 `detect_current_language` tail-slice + `lang_id` SOT forward 재조합으로 구현·계측(진단전용, 게이팅 미연결). bong1(웃음)+ytn2(필러) tail(2.0s) 513샘플 + bong1 고정밀 290샘플 **전부 no_speech=0.000000**. 고정밀 진단에서 `ns_logit`이 -10.1~-10.5로 변동(forward 정상)·top_id=50360 top_p≈0.52(SOT 분포 정상)임에도 no_speech 토큰만 logit ~-10 고착 → **turbo no_speech 헤드가 tail 내용 무관 degenerate**임이 확정. **Exp-164의 "롤링버퍼 오염" 진단을 정정**(tail만 봐도 0 → 원인은 버퍼가 아니라 헤드). **Layer 3b no_speech 계열(전체버퍼·tail 모두) 폐기 확정 — 남은 경로는 웃음 전용 비-ASR 분류기 뿐(별도 설계 세션).**
- **[E4·규명][필터][base진단·재확인필요]** **QualityGate(avg_logprob<-2.0) 부당드롭 = 0%**(Q1 규명, Exp-154 하니스). 억제 텍스트 전수 분류(bong1 46·ytn2 33·sbs1 14) 결과 전부 ① 비음성 마커(laughter/applause/speaking/AUDIO) ② 문장부호·단일문자 ③ **최종 전사에 이미 존재하는 중복 재디코딩 조각** ④ 환각조각뿐 — 정상 한국어 유실 없음. → **언어별 logprob 임계·드롭→재디코딩 수정 불필요**(사용자 확인). ytn2 회차분산은 QualityGate가 아닌 다른 원인(ForeignLang 혼란/재디코딩 churn/실오인식). logprob 분포 자체가 모델마다 다르므로 turbo에서 재확인 필요. → **[E5·재확인(Exp-173)]** turbo에서도 결론 유지: 억제 96건(bong1 46·ytn2 33·sbs1 17) 전수 dup(중복재디코딩 생존)/punct/filler 라벨링 결과 **순유실(정답 포함 AND 최종전사 미포함)은 단 1건**(ytn2 "verified"@-2.05, 임계 바로 아래). 나머지 = dup 46·punct 38·filler 7·other(환각조각) 4. 부당드롭률 ≈1%(1/96). **주의: Exp-172 STATE 1행/LOG의 "QualityGate=③ 최다기여" 서술은 dup 생존 미체크로 인한 오귀속 — Exp-173에서 정정**(bong1 who/생각을/plus/a lot/for 등은 전부 최종전사에 생존한 dup). turbo 실제 ③ 유실 경로는 [Retract] 철회 후 재디코딩 미복구·held/UTF-8 손상·세션초입 buffer(Exp-173 참조).
- **[불변][필터/반복][모델무관·유지]** master 유지 베이스라인 필터 = **Exp-002**(cross-batch stateful 반복)/**Exp-028**(단일음절 연속반복 억제+context 리셋)/**Exp-057**(배치 내 4-word 반복 드롭). 신규 언어특화 하드코딩보다 backend 대안 우선. `_filter_repetitions()`는 단일 `update()` 배치 내부만 동작 → cross-batch 반복은 stateful 필터 필요.
- **[불변][측정·레벨][모델무관·유지]** 입력 볼륨은 정상 범위(±12dB)에서 WER에 유의미한 영향 없음(Exp-157) — Whisper log-mel 창별 자기정규화(audio.py:155)로 절대 레벨 둔감, 회차 변동성(10~14pp) ≫ 레벨 효과(~3pp). ytn2를 −37.9 LUFS까지 낮춰도 VAD 미검출 없이 유지. bong1(핫/소스클립)만 감쇠 미세개선. **서버측 볼륨 정규화(AGC)는 미적용**(측정상 WER 이득 없음; 잔여가치는 배포 마이크 극단 오설정 로버스트니스뿐 — 이번 스윕 미측정). 게인 스윕(`eval.py --gain-db`)·`verify_loopback` 유니티 게인 검증(−20dBFS±1dB)은 진단 인프라로 상비 — 이 인프라 자체는 모델 무관.
- **[E5·신규][모델무관·유지]** `SimulStreamingASR.__init__`의 `model_dir` 배선 버그(Exp-158) 수정 후, 다른 백엔드(qwen3/voxtral)와 동일하게 `model_dir or model_path` 폴백 패턴 적용됨 — 향후 SimulStreaming 관련 모델 경로 변경 시 이 패턴 유지.
- **[E5·규명][base전용·재검증]** turbo 전환으로 코드스위칭 실패모드 자체가 변함: ytn2에서 방송 클로징류(Exp-157서 "MBC 뉴스…") 대신 **"Thank you" 연쇄 필러 환각**이 우세하게 관측(Exp-158) — turbo가 불확실 구간에서 "더 그럴듯하게" 필러를 생성하는 경향으로 추정. base용으로 튜닝된 코드스위칭 스캐폴딩(언어전환 프로토콜·PLC)이 이 신규 실패모드에 적절한지 재검증 필요.
- **[E5·규명][모델무관·유지]** "Thank you" 필러 폭주가 **bong1·ytn2에 이어 ytn1(held-out)에서도 재현**(Exp-159) — turbo 전반의 일반적 실패모드로 확정(3개 파일 공통). 단 ytn2의 **catastrophic 콘텐츠 대체(방송클로징 환각)는 쌍둥이 ytn1에서 재현되지 않음** — 코드스위칭 일반의 문제가 아니라 ytn2 파일 고유의 음향/정렬 난이도(`audio-feature-analysis` 미머지 도구의 앵커율 46% 발견과 정합)가 만든 최악 사례로 추정.
- **[E5·채택][성능][모델무관·유지]** `audio_max_len` 기본값 **30.0→15.0 채택(Exp-161)** — sbs1 lag 41s(Exp-158)의 원인은 매 infer() 호출마다 버퍼 전체를 재인코딩하는 비용 누적으로 규명. 15.0으로 낮춰 lag 2s대 안정화 + 3파일 모두 WER median·max·stdev 개선(특히 sbs1 23.2%→14.9%, stdev 10.6→1.5). 원인이 "인코딩 비용 대 버퍼 크기"라는 압축가능한 계산량 문제이므로 모델(base/turbo) 무관하게 유효할 가능성 높음(단 base에선 미검증).
- **[E5·Exp-168 규명·모델무관·유지]** Exp-90의 "Thank you" 필러 재검증 완료(부분): ytn2에서 EN↔KO 전환 경계 직후 필러가 실제 발화를 통째로 삼키는 메커니즘 확정 — ① 기존 4개 필터(QualityGate·BatchRepeatFilter·CrossBatchFilter·DRY)는 전부 "완전동일반복" 또는 "한국어 전용"을 전제해 "영어+변주" 필러에 원리상 무력(`BatchRepeatFilter`의 `[가-힣]` 조건이 결정적 사각지대). ② `new_speaker()`의 eager 언어감지가 화자전환 경계시각과 무관하게 버퍼 끝 "마지막 1.5초"를 무조건 잘라, 새 화자 발화 직후엔 직전화자 오디오로 오판하는 별개 메커니즘도 확정. 두 방어선(언어-스크립트 불일치 게이트 + since_offset 시각보정)으로 ytn2의 두 관측 경계 모두 해결(WER 43.8%→26.1% max). **단, bong1·held-out 등 다른 파일의 필러 발생 여부는 이번 조사 범위 밖**(§ Req-3/CASE3에서 재검증 필요) — "Thank you 필러가 turbo 전반의 일반적 실패모드"라는 Exp-159 결론 자체를 뒤집는 것은 아님.
- **[E5·Exp-169 규명·모델무관·유지]** CASE3 script-agnostic 앵커 반복게이트(gap-tolerant 클러스터링, MIN_COUNT=4·MAX_GAP=5·MIN_CONCENTRATION=0.6)는 짧고 규칙적인 storm(ytn2 "김정은"×4류)은 완전 억제하나, **가변 길이 변주구가 낀 storm(예: "Thank you [긴 변주구] Thank you" 반복)은 앵커 사이 gap이 5단어를 넘어 서브클러스터(예 3+2)로 쪼개져 MIN_COUNT 문턱을 피해가는 사각지대가 실측(N=3)으로 확인됨** — Req-2가 해결했던 ytn2 두 경계(안보리결의 직후·정경두 국방장관 직전)에서 이 사각지대형 storm이 각 1/3 빈도로 재현, 목표 문장 전체를 완전 대체(catastrophic swallow). 게이트 로그(`AnchorRepeatFilter`)가 두 재현 지점에서 전혀 발동하지 않아 게이트 자체가 원인이 아님은 확정(원래도 존재하던 잔존 위험). 향후 이 사각지대(가변 변주구 storm) 대응 시 MAX_GAP 단순 확대보다 "근접 시간창 내 앵커 총 등장" 같은 재설계 고려.

- **[E5·Exp-171·모델무관·유지][언어전환경계]** **언어전환 경계 철회(retraction) + 화자전환 keep_secs 채택** — diar 이벤트 지연(~1-2s) 동안 구언어 잠금으로 커밋된 잔존/접착 토큰을 `LanguageSwitch` 마커 방출 직전 2구역 규칙(재디코딩구간=언어매치만·경계직전=+스크립트불일치)으로 철회(`tokens_alignment.py _retract_stale_language_tokens`), `refresh_segment`의 고정 `[-2:]` 컷을 `keep_secs`(경계-앵커 유지)로 교체 + `global_time_offset`을 `self.end - segments_len()`으로 재계산(`backend.py new_speaker`). ytn2 §3.8 최우선 대폭개선(median -10.9pp), bong1/sbs1도 median 개선. **bong1 max만 게이트 초과**(R2 1회, 기존 필러/웃음 실패모드와 정성일치 — 회차변동 판단, 사용자 승인 채택). `RETRACT_EPS=0.05`·`MARGIN=0.3`/`MAX_KEEP=5.0`은 **잠정값**(`docs/SENTENCE_FINALIZATION_LOGIC.md` §5 "잠정" 태그) — 추가 실측 후 보정 여지. **철회 하한은 Exp-174에서 정정**: 고정 `KEEP_SECS+1.0`(3.5s) 하한이 화자전환 트림(예 keep_secs=0.61s)보다 넓게 스캔해 재디코딩 불가능한 서두 토큰까지 철회(bong1 "You don't understand" 4단어 유실 확정) → `LanguageSwitch.retract_floor`(재디코딩 창 시작=`global_time_offset`)를 하한으로 우선 사용, `None`(하위호환)일 때만 기존 계산으로 폴백. 상세는 Exp-174 참조.
- **[불변][측정][모델무관·유지][2026-07-18]** **정답 스크립트 명명규약 단일화 + kor1~3 문장분리 재정제(사용자 정정, 코드 무변경)** — `test_data/*_speak,sentence_sperate.txt`(신형식)를 `test_data/<name>.txt`로 이전·구 접미사 파일 삭제(전 9개 파일 통합, `eval.py` 파서 동기화 완료). 이와 함께 **kor1~3 정답의 문장분리를 재정제** — 쉼표/구 단위로 과분할돼 있던 문장을 온전한 문장 단위로 재병합(예: kor1 8줄→1줄 병합). **향후 kor1~3(ko 테스트셋) 측정은 이 정정된 정답 기준**이며, 정정 이전(2026-07-18 이전)에 측정된 kor1~3 WER·F1(예: Exp-178~184의 kor1~3 스크리닝 수치)은 **문장 경계가 다른 구 정답 기준이므로 문장분리 F1 직접 비교 불가**(WER은 문장경계 무관이라 영향 없음). 신규 kor1~3 측정 전 이 사실을 인지할 것.
- **[E5·Exp-170·모델무관·유지][출력계층]** **온점 형태소 분할 채택** — Whisper 마침표를 문장 경계로 쓰되 한국어 종결어미(`KO_FINAL_SUFFIXES`, bare 단음절 제외로 명사충돌 방어)·영어 약어(`EN_ABBREV`+다음단어 대문자) 판별로 거짓 마침표(어간·조사 중간 온점·소수점·약어)를 걸러 분할(`sentence_boundary.py` 신설). diar 병합 재합침 방지는 `PuncSegment.punct_boundary`(병합조건·전파·라벨 3곳). **출력 계층 전용이라 WER 중립**(단어 시퀀스 불변) → 디코더 파라미터 결론 무영향(**epoch 미bump**). **F1 지표 한계 규명(중요)**: 정답이 `scripts/eval.py:parse_reference_sentences`로 **빈 줄(화자전환/문단) 기준** 분할이라 문장(온점) 경계를 credit 못 함(§3.3 온점 2순위 metric 미구현) — 문장분할을 늘리면 hyp 경계↑로 precision↓ → F1 **오히려 하락**(sbs1 R3 F1=0.0%인데 전사는 14개 정문). **∴ 문장분리 기능은 현행 F1로 정량평가 불가 — 온점 경계 F1 구현이 후속 과제**. **[regime v2 supersede]** 이 "정량평가 불가"는 **구 regime 한정** — 신형식 정답(`_speak,sentence_sperate.txt`)의 화자 블록 내 줄바꿈으로 **문장분리 F1**을, `[spk]` 전환으로 **화자분리 F1**을 별도 산출하는 경로가 마련됐다(측정·구현 = [docs/TRANSCRIPTION_REQUIREMENTS.md](docs/TRANSCRIPTION_REQUIREMENTS.md); 우선순위 화자분리 F1 > WER > 문장분리 F1). 채택 근거 = 목표(문장단위 확정 §3.3)·정성정확·WER중립(사용자 승인). bong1 WER max 36.0(웃음 필러환각 변동)은 출력계층 기능과 무관.

- **[불변][측정][모델무관·유지][Exp-186]** **단일화자(화자경계 0개) 참조파일의 화자분리 F1은 all-or-nothing 아티팩트** — `metrics.py:compute_speaker_sentence_segmentation`은 ref에 화자경계가 0개면(kor1/kor2/kor3처럼 `[spk1]` 단일블록) hyp의 문장분할이 100% 완벽할 때만 F1=1.0(trivial), 단 하나라도 어긋나면 즉시 F1=0.0이 되는 이진 구조다(실측: kor2/kor3는 문장분리F1=100%인 회차만 화자F1도 100%). **Exp-185 "다음 가설 2"(kor1/kor2 화자F1이 SHS 무관 0% 고착 원인) 해소** — Sortformer·게이트 상호작용이 아니라 이 메트릭 구조 때문. kor1~3 화자F1을 채택 게이팅에 쓰기 전 이 한계를 인지할 것(상세 = docs/research/AUTO_MODE_KOREAN_PERF_ANALYSIS_20260718.md §3.4).

- **[E5·Exp-193·모델무관·유지][언어전환경계·파라미터]** **frame_threshold는 KO→EN 경계 구언어 꼬리유실을 직접 제어하는 레버**(배포 "갱신하겠습니다"→"갱신하." mid-word 절단·dev 완화형 "합의점에 이르렀습니다"→"합의점에."의 근본원인). 매 infer가 버퍼끝 frame_threshold(기본25≈0.5s)를 미디코딩으로 남기고, 언어전환 리셋(`_apply_detected_language`의 트림+토크나이저 스왑+`init_tokens`)이 그 **미커밋 KO 꼬리를 폐기**(다음 infer가 EN 토크나이저로 재디코딩→garbage→ScriptMismatch 드롭)해 순유실. **frame_threshold↑(25→35)가 정상 디코드로 꼬리를 경계 前 커밋시켜 ytn2 "합의점에" 3/3 복원·WER 중립**, 단 전역 상향은 모든 커밋 지연→다화자 화자전환 분리 저하(bong1 화자F1 76.5→62.9, **1순위 회귀**)라 전역 기본값 불가·**배포 opt-in 레버**(`--frame-threshold 35`). GPU 배치크기(빠른 GPU=작은배치→꼬리가 미디코딩 구간에 걸릴 확률↑)가 심각도 결정→배포5090에서 잦음. **경계에서 사후 flush(구언어 강제 디코딩)는 원리적으로 늦음**: 전환이 new_speaker(화자전환) 주도라 발동 거의 안 되고(ytn2 0/9) boundary 지연으로 KO꼬리·EN서두가 버퍼에서 얽혀 garbage(Exp-193 is_last flush 실증 실패, `exp/boundary-tail-flush` 미머지 기각 — **재시도 금지**). 재조정(Exp-192)도 미커밋 꼬리는 시야 밖. **[E5·Exp-194·추가검증]** frame_threshold 외 대안(조건부/국소 ft·경계
슬라이스 재디코딩 D1·문법 확정유예·화자전환 지연보정) 4클러스터 병렬탐색 결과 D1(new_speaker refresh 직전
KO-슬라이스, `decode_tail_slice`)이 이론상 최유망(전역비용0)으로 도출됐으나 **실측 스크리닝에서 기각** — 삽입점
이동으로 발동 자체는 성공(new_speaker 지배경로 포착)했지만 목표 경계에서 정작 무로그 스킵(진단갭) + 화자F1
다수 회귀(bong1/sbs1/ytn1 전부 66.7%로 하락) + bong1 어절중복·sbs1 신규유실/환각 관측. **"전역비용 0" 설계가정이
실측에서 성립하지 않음** — 경계-국소 재디코딩 자체가 화자F1을 흔든다(원인 미규명). 코드레벨 시도 2회(Exp-193
is_last flush·Exp-194 D1) 모두 실패로 **frame_threshold 배포 레버(opt-in)가 최종 결론** — 이 계열(경계에서
구언어 강제 재디코딩/flush, 삽입점 무관) **추가 진단계측 없는 재시도 금지**.

## 빠른 참조

> **Exp-001~157 = base 기질 시대(2026-07-05 이전)** — 전체 표는 [EXPERIMENTS_LOG.md](EXPERIMENTS_LOG.md) 상단으로 이관됨(STATE 예산 절약). 구조 변경(Exp-139·143·150·151·152·153)만 유효, 파라미터 결론은 전부 재검증 대상 — 위 "코드 세대(Epoch)" 절 참조.
> **Epoch 열**: E6 = turbo 기질 + new_speaker 동일언어 스킵(2026-07-22~). E5 = turbo 기질(2026-07-05~2026-07-22, 이력). E1~E4 = base 기질(무효, 이력).
> **언어모드 열(2026-07-17 도입)**: 해당 Exp가 개선·측정한 세션 언어모드(CLAUDE.md §3.2) — `auto`/`ko`/`en`. 이 열 도입 이전 기록된 행(Exp-183 이하)은 전부 표준셋(bong1/ytn2/sbs1) 기준 auto 암묵 측정이었으므로 소급 표기 없이 `—`로 둔다. **Exp-184부터 필수 기입.**

| Exp | Epoch | 언어모드 | 날짜 | 변경 | bong1 WER med | ytn2 WER med | sbs1 WER med | 판정 |
|-----|-------|---------|------|------|--------------|-------------|-------------|------|
| Exp-206 | **E6** | ko | 2026-07-23 | **kor3 "감사합니다" 클로징 필러 재현 시도 + Exp-199 이전 커밋(`2957cc7`) 대조측정**(코드변경 없음, kor3 단독 `--repeat 4`×2회차 — 신코드/구코드) — T2 원목표(§2 계열②) 실측 재검증 | — | — | (kor3만 측정) | 📋 **계측완료(채택/기각 비대상) — 계열② 실재여부 불확실성으로 결론**. "감사합니다"가 디코더 내부 후보로는 신코드 4/4·구코드 4/4 재현되나 **실제 커밋(`Output:`)에는 8회 전부(신4+구4) 미방출**. 최초 가설("Exp-199/200이 부수효과로 억제")은 **대조측정으로 반증**(구코드도 동일하게 억제) — 억제 메커니즘이 그 수정 이전부터 존재. 원 진단("생성·방출")이 이번 8회 표본과 배치 — 간헐적 현상이라 미포착됐거나, 원 진단이 DEBUG 후보 라인을 방출로 오인했을 가능성 둘 다 남아 **불확실성 자체가 결론**(사용자에게 원 로그 라인 재확인 요청). T2 게이트는 커밋 안 되는 텍스트엔 발동 불가하므로 조건 좁히기 보류. 상세 = EXPERIMENTS_LOG.md Exp-206 |
| Exp-205 | **E6 후보(미머지)** | ko+auto | 2026-07-23 | **T1+T5 결합 검증**(코드 신규작성 없음 — `git cherry-pick 9c17e0f 80f4bbd`, 충돌 0건, 752 pass) — 두 fix가 서로 다른 브랜치에서 독립 개발돼 상호작용 미검증이었던 것을 확인 | 23.5%(N=1) | 24.1%(N=1) | 12.5%(N=1) | 📋 **계측완료(채택/기각 비대상) — 상호작용 문제 없음 확인**. ko 스크리닝: kor1 NewSpeaker 3회, 3/3 전부 T1 스킵 발동(Refresh는 무관한 정상 3회) — T5(노이즈 이벤트 감소)+T1(남은 이벤트 재디코딩 비용 제거)이 설계대로 상호보완. auto는 전부 Exp-161 게이트 이내(단 N=1). T1의 lang_locked 분기는 auto 세션에서 예상대로 비활성 확인. 정식 채택확정(--repeat 3)은 T1·T5 개별 머지 승인 후 별도 필요. 상세 = EXPERIMENTS_LOG.md Exp-205 |
| Exp-204 | **E6 후보(미머지)** | ko+auto | 2026-07-23 | **ChangeSpeaker dispatch 최소길이 필터**(`audio_processor.py`, `exp/hallu-t5-changespeaker-dispatch-filter@80f4bbd`, 환각루프 T5, §3 예비항목에서 승격) — Sortformer 원시 세그먼트 화자변화를 길이검사 없이 그대로 dispatch하던 것이 kor1 flip-flop 버스트(NewSpeaker 8연속)의 근본원인이었음을 실측 규명(Exp-188 완화책은 이 경로에 적용 안 됨, `[SpeakerAttribution]` 0건 확인). 0.5s 미만 세그먼트는 dispatch·상태갱신 모두 스킵. TDD 8건 GREEN, 750 pass. ko 스크리닝: kor1 NewSpeaker 22~30회→4회 | **N=5: 33.7%(med)/36.1%(max, 게이트 초과 +5.6pp, stdev 큼 26.8~36.1)** | 18.7%(med)/21.2%(max, 대폭개선 — Exp-106 유사회귀 미재현) | 10.7%(med)/11.3%(max, 대폭개선) | 🟡 **조건부 채택권고(브랜치 커밋, 머지는 사용자 확인 대기)** — ytn2·sbs1 명확 개선. bong1은 N=3→N=5 확장 후에도 max 게이트 초과 지속(오히려 폭 더 벌어짐), 단 baseline(stdev 0.7%)과 달리 이번 분산 자체가 매우 큼(stdev 대) — 최악회차 정성분석 결과 기존·별개 웃음구간 필러환각(Layer 3b 미해결 이슈) 패턴이며 diarization 관련 신규 아티팩트 없음. median이 baseline(29.3~30.5%)보다 높아졌을 가능성(33%대)도 사용자 확인 시 함께 고지 — 인과 완전배제는 불가해 자율기각 대신 에스컬레이션(Exp-176/199/200 선례). 상세 = EXPERIMENTS_LOG.md Exp-204 |
| Exp-203 | **E6** | auto | 2026-07-23 | **음차 경계 환각 고아 토큰 계측**(`[PreSwitchOrphan]`, `exp/hallu-t3-preswitch-orphan-probe@57e8b71`, 계측전용·코드변경0, 환각루프 T3) — 언어전환 마커 삽입 직전 최근 커밋 run을 로깅(임계값 없음) | 11건(발동) | 9건(발동) | 2건(발동) | 📋 **계측완료(채택/기각 비대상) — 자동 임계값 미도출**. 22건 전수 대조: sbs1은 목표결함("사태라") 미재현(정상 대조군), **ytn2에서 신규 경계환각 포착**(`John Gyeong` = 국방장관 이름 음차오번역, sbs1 "사태라"와 동일 계열 신규 사례) + 2건은 probe 스냅샷 이후 철회된 것으로 확인(`You`→최종`Let's.`·`Minister Liu:`→최종`Prime.`), bong1 1건은 혼재(왜곡+정상). 길이/gap 수치로는 환각·정상 분리 안 됨(둘 다 넓게 분포) — 메커니즘은 유효하나 게이트는 미설계. 상세 = EXPERIMENTS_LOG.md Exp-203 |
| Exp-202 | **E6** | ko+auto | 2026-07-22/23 | **침묵 클로징 환각 섀도우 계측**(`[SilenceHalluProbe]`, `exp/hallu-t2-silence-hallu-probe@0348f1d`, 계측전용·코드변경0, 환각루프 T2) — VAD 침묵개시 강제flush(`is_last=True`, quality_gate 스킵 경로)에서 attention_reached_end∧frame_advance≈0∧텍스트커밋을 후보로 로깅 | 0건(would_hold) | 1건(`control`@7.22s) | 0건 | 📋 **계측완료(채택/기각 비대상) — 게이트 배선 금지**. would_hold 총 3건(kor3 2·ytn2 1) 전수 라벨링 = **오탐 3/3(100%)** — 전부 짧은 정상 단어의 버퍼경계 부분방출(TokenTrace 대조로 continuation 시도 흔적 확인, 새 텍스트 지어낸 게 아님). 후보 시그니처가 특이도 부족(모든 미세 pause에서 발동하는 `is_starting` 트리거 특성상 불가피) — 조건 좁히기(long_silence 연동·최소 run 길이·hold-continuation 확인) 필요. 상세 = EXPERIMENTS_LOG.md Exp-202 |
| Exp-201 | **E6 후보(미머지)** | ko+auto | 2026-07-22 | **lang_locked 세션 new_speaker 경계 재디코딩 무조건 스킵**(`backend.py new_speaker()`, `exp/hallu-t1-lang-locked-skip@9c17e0f`, 환각루프 T1) — lang_locked 세션은 eager 재감지를 안 돌려 "동일 언어 확정" 스킵 조건(Exp-196)이 구조적으로 절대 발동 못 해 매 화자전환마다 무조건 `refresh_segment` → 중복방출(kor1 R3=NewSpeaker32·Refresh34·WER32.2%, R4=NewSpeaker60·Refresh63·WER35.1%, goal 진단 인용). lang_locked이면 재감지 없이 곧장 스킵 경로로 직행하도록 수정, auto 경로 무변경. TDD(RED→GREEN 2건), 전체 744 pass·ruff clean. **kor1/kor2/kor3 median(ko, N=3)**: 11.7%/17.9%/30.5% | auto 무회귀 N=1: 30.1% | 14.3% | 13.7% | ✅ **채택권고(브랜치 커밋 완료, 머지는 사용자 확인 대기)** — kor1 NewSpeaker 22~30회 발동 회차에서 **Refresh 3~4회로 0-firing 회차와 동일 유지**(수정 전이면 발동횟수만큼 폭증)+WER이 오히려 개선(11.1~11.7% vs 0-firing 14.0%)+중복 0건·Case B 0건 실측 확인. auto 3파일 Exp-161 게이트 전부 이내. kor1 화자F1 0.0(firing 회차)은 Sortformer flip-flop 오탐(양쪽 코드경로 동일 동작, 이 fix와 무관)으로 규명 — 별개 이슈(§T5). ko baseline 미수립이라 정식 게이트 비교는 kor 확정 재측정 후. 상세 = EXPERIMENTS_LOG.md Exp-201 |
| Exp-200 | **E6** | ko+auto | 2026-07-22 | **UTF-8 반토막 음절 커밋 오염 근본수정 — 클린 커밋 절단(Direction A)**(`align_att_base.py`, `CONTEXT_CLEAN_COMMIT_ENABLED=True`, **master 머지 `76150ca`**). Exp-199 seam 수정이 방출/hold 경로만 첫 `�`에서 멈추게 했으나 **커밋 경로(`tokens.append`)만 반토막 `�` 토큰을 넘어 전진**해 high-water mark를 오염 → 완성 바이트 재디코딩 불가 → drop(영구유실)이 잔존. 커밋 직전 `new_hypothesis`를 첫 `�` 단어 앞 클린 프리픽스로 절단(순수 subtractive, `_clean_commit_hypothesis`) → mark 청결 → 다음 청크가 오염 없이 재디코딩 → seam 게이트가 stale 정리. 짝지음 A/B(ON/OFF, SEAM=True 공통), 스크리닝 1회. TDD 신규 7 + 기존 13 GREEN, 전체 701 pass. **표적: [UTF-8 Fix] Dropping 이벤트 총 14→2**(kor2·kor3·ytn2·sbs1→0), 유실구 `보장`·`로봇 등`(kor3)·`플랫폼`(sbs1) 정확 문맥 복구, **FFFD 방출 0**(Case B 0·미미디어 중복 0). ko: kor1 18.7→14.6·kor2 21.4→17.2·kor3 39.7→29.1%(화자F1 0→1.0). **epoch bump 없음**(Exp-199 동류 UTF-8 커밋계층 정합성 수정, 파라미터 트레이드오프 무영향). 상세 = EXPERIMENTS_LOG.md Exp-200 | 25.3→**33.4%** ⚠️(회귀·비-catastrophic 꼬리절단) | 14.8→**12.3%** | ~12.5→**~8.9%**(플랫폼 복구) | ✅ **채택(master 머지 `76150ca`)** — 표적지표·ko 명확 개선. bong1(§3.8 최우선) auto WER +8.1pp 회귀는 §4 사용자 질의로 에스컬레이션했고 **사용자가 실사용 관찰상 만족으로 머지 승인**(bong1 `--repeat 3` 확정측정은 중단·생략). 1회 스크리닝 근거 |
| Exp-198 | **E6** | auto | 2026-07-22 | **Stage 1 섀도우 게이트 지속문턱 `STAGE1_MIN_DURATION` 1.0s→0.8s 완화**(`align_att_base.py` 상수 1개, `6868c2d`, 회귀테스트 3건, pytest 674→677 pass·1 skip) — 게이트 로직·섀도우 불변식 무변경(`_apply_detected_language` 신규호출 0건). 계기 = Exp-197 `blocked_g5`를 프로브 단위로 열어보니 **확정 참양성 1건이 T에 0.04s 못 미쳐 막혀 있었다**(bong1 `bong1.txt:43` 플라스틱 구간이 `Plastic, sorry`/`Malang mal`/`awesome thing to make`로 뒤집힌 언어잠금 실패, 버스트 t=91.20~92.16 k=1→4·p_opp 0.9797→0.9996·seglen 4.36→5.32·**dur=0.96**, G3/G4/K≥3 통과·T만 차단). **주 증거 = 오프라인 리플레이**(게이트가 프로브 스트림의 순수함수라 로그만으로 임의 상수 재계산, Exp-197 4파일 `[Stage1ShadowStats]` 카운트 정확 재현으로 검증 — 함정: `infer(is_last=True)`가 그 호출의 마지막 프로브 처리 *전에* 요약 방출 → 마지막 1개 제외해야 일치) | 24.7%(참고치, N=1, 행동무변경) | **11.1%**(N=2 재측정 median, 8.4/13.8·stdev 3.8 — 1차 run은 `FFmpeg read timeout`·lag 58.70s·프로브 150 반토막의 하니스 stall이라 WER 69.0%/화자F1 26.7% **폐기**) | 8.9%(참고치, N=1, 행동무변경) | 🟡 **계측 개선(섀도우 유지) — 리플레이 근거로 T 1.0→0.8 채택.** T 스윕(would_fire bong1/ytn2/sbs1/ytn1) 1.0→0/0/0/15 · 0.9→1/0/0/16 · **0.8→1/0/0/16** · 0.7~0.5→2/0/0/17 · 0.0→2/0/0/19이며 **에피소드 커버리지가 T=0.5~0.9 동일**(발동 지연만 변화)이라 knife-edge 아님. **오탐 위험 0 확인** — ko/en 16세션(kor1~3×4+eng1×4)을 **G1 우회**(단일언어 내용이 auto로 들어온 worst case)로 리플레이해 T=0.0 포함 전 구간 would_fire=0, `p_opp≥0.97` 프로브 최대 seglen 1.02s vs G3 2.0s(여유 0.98s) → **차단은 전적으로 G3의 몫·T 기여분 0**. 실측은 end-to-end 확인만(ytn1 fire 2건·회귀 없음) — **로깅 전용 diff이므로 Exp-197 대비 WER/F1 차이는 전부 회차간 분산, 개선 주장으로 인용 금지**. **한계: would_fire 실현편차 큼**(같은 파일·같은 코드에서 ytn1 15건→2건, bong1 리플레이 1건→실측 0건) — 승격 시 단회 측정으로 이익/해악 귀속 불가, N≥3+발동카운트 짝지음 필수. **실제 트리거 배선은 여전히 사용자 판단 대기**(master 미머지, `exp/lang-lock-stage1-duration`). epoch bump 없음(디코딩 경로 무변경). 정본 = `docs/research/SOT_LANG_PROBE_STAGE0.md` §12 · 인계 = `docs/backlog/LANG_LOCK_STAGE0_HANDOFF.md` §4-0-4 |
| Exp-199 | **E6** | ko+auto | 2026-07-22 | **UTF-8 seam 수렴 게이트 + 방출/보류 계약 정렬**(`align_att_base.py`, `SEAM_CONVERGENCE_FIX_ENABLED=True`) — 스트리밍 배치 경계에 한글 멀티바이트가 걸려 hold-retry 2회 실패 시 통째 폐기되던 중간단어 유실(BUG B) 완화. **표준 auto셋(bong1/ytn2) 미측정** — 이번 스크리닝은 kor1~3(ko)+sbs1(auto)만: kor1 18.1→14.6%·kor2 18.6→18.6%(텍스트 완전동일, drop 2→4나 무해)·kor3 39.1→33.1%(drop 4→0). 사용자 시간제약으로 N=3 확정측정·bong1/ytn2 회귀검증 생략, 사용자 승인으로 조기 머지(`f9f9cc5`). pytest 694 pass·1 skip | 미측정(다음 세션 회귀확인 필요) | 미측정(다음 세션 회귀확인 필요) | 10.1→13.7%(1차 sbs1은 컴퓨터정지로 오염·폐기, 재측정값. 악화분은 별개 emit버그 귀속, seam 무관 — 로그 확인) | 🟡 **스크리닝 방향신호로 조기 머지** — UTF-8 drop 표적지표는 4파일 중 나쁜 텍스트 변화 0건, Case B 0건. **bong1/ytn2 무회귀 미확인 상태로 머지됨** — §3.8 우선순위(ytn2·bong1) 재확인 필요. 원본 JSON/로그/전사는 워크트리 삭제 시 소실(교훈 기록됨). 상세 = EXPERIMENTS_LOG.md Exp-199 |
| Exp-197 | **E6** | auto | 2026-07-22 | **언어잠금 Stage 1 섀도우 게이트 실측(안 B 완료)** — 게이트 G1~G7(비-auto 가드·언어미감지·최소버퍼`seglen≥2.0s`·`p_opp≥0.97`·연속K≥3∧지속T≥1.0s·쿨다운3.0s·타 트리거 진행중 배제) + 증거 리셋 훅 구현, `would_fire`만 로깅하고 `_apply_detected_language` 미호출(diff 신규호출 0건 확인, 섀도우 불변식 유지) — **디코딩 로직 무변경**. 측정 중 `[Stage1ShadowStats]` 세션요약 중복억제가 `would_fire`만 비교해 `blocked_by` 누적치가 첫 `is_last`(프로브 2회째) 이후 갱신 안 되는 버그 발견·수정(`d6bd66a`, `_log_lang_drift_stats`의 `probes` 총호출수 중복억제 관례로 통일, 회귀테스트 1건). 수정 후 v2로 auto 4파일(bong1/ytn2/sbs1/ytn1) 재측정(N=1 스크리닝) | 28.9%(참고치, N=1, 행동무변경) | 13.3%(참고치, N=1, 행동무변경) | 11.9%(참고치, N=1, 행동무변경) | 🟡 계측 완료 — bong1/ytn2/sbs1 would_fire 0건, held-out ytn1 15건(K=5→19로 이어지는 1개 연속 에피소드, 시간창이 ytn1 기존 언어잠금 실패 구간·LMR_ko 19.7%와 일치해 정성 확인상 참양성) — Stage 1 실제 게이트 적용(트리거 배선)은 사용자 판단 대기(master 미머지). epoch bump 없음(디코딩 경로 무변경). 정본 = `docs/research/SOT_LANG_PROBE_STAGE0.md` §11 · 인계 = `docs/backlog/LANG_LOCK_STAGE0_HANDOFF.md` §4-0-3 |
| Exp-196 | **E5→E6** | auto | 2026-07-22 | 화자전환 시 동일언어 확정 경계 재디코딩 스킵(samelang 머지, 별도 세션 구현·측정·머지 후 이 세션은 반영·정리만) — `fix/samelang-no-refresh`(`3ad14a6`=`47fd76c`+`4448288`). eager 언어감지가 기존 언어와 동일함을 확정하면 `refresh_segment` 경계 재디코딩을 스킵하고 화자귀속만 갱신(리셋 전체 생략이 아님), 쿨다운으로 eager=None인 구간은 `_last_eager_lang_result` 캐시로 스킵판정에만 재사용. **Exp-155(E4)와 같은 가설, 두 번째 시도** — Exp-155는 리셋 전체를 생략하는 넓은 범위+base 기질에서 시도해 bong1 new_speaker 15/15 100% 스킵→진짜 다른 화자 블렌딩→화자F1 -4.1로 기각됐다. 이번은 재디코딩 호출만 스킵하는 좁은 범위+E5 turbo 기질이라 그 실패 지점을 피해간다 | 73.7→78.8%(화자F1) | 15.8→12.3%(WER)·80.0→94.7%(화자F1) | 회귀없음(80~100%밴드) | ✅ 채택(머지 완료, 커밋메시지 N=1 스크리닝 인용) — ko/en 세션 영향 없음(`lang_locked` 분기 early return 미진입, Exp-195 ko/en 수치 23건·0.61%·게이트0/23 유효 유지) / auto 세션은 `refresh_segment` 미호출로 seglen 거동이 바뀌어 Exp-195 auto 수치(ytn1 25건 등)와 직접비교 불가. **epoch E5→E6 bump**(new_speaker 실패모드 변경) — 상세 = EXPERIMENTS_LOG.md Exp-196, epoch 근거 = 본 파일 "코드 세대(Epoch)" 절 |
| Exp-195 | **E5** | ko+en+auto | 2026-07-21 | **언어잠금 Stage 0 — 계측·진단 전용(디코딩 행동 변경 0)**. ① **LMR 진단지표**(`compute_language_mismatch`, 채택 게이트 아님) — bong1 10회 소급에서 언어잠금이 WER median의 14%·최악 24%를 먹음(LMR_ko med 15.4%=4.38%p, max 37.4%=10.27%p, corr +0.724, **en 잠금 단방향**) ② **SOT 사후분포 프로브**(`[SotLangProbe]`) — 살아남은 신호는 `p_opp` 단독. `p_translate` 723프로브 전부 0(task누출 가설 S3 사망)·`resid`/`H_lang` ytn2 방향역전·`p_nospeech` 전배치 0으로 폐기 ③ **표본확대**: 단일언어 세션 오탐률 측정(`exp/lang-lock-stage0@8f20dd5` 미머지) | 30.06%(N=10 소급) | 26.6%(R1) | 11.3%(R1) | 🟡 **계측 채택 권고 / 개입(Stage 1) 정지 — 사용자 판단 대기**. ko 세션에서 `p_opp≥0.97` 발동 **5건**(순수 오탐 2건 확정: kor2 `먼저 육군`·kor3 `원 거리 타격`)이 실측돼 사전 등록 진행조건("단일언어 세션 정상전사 발동 0건")을 불충족 — Exp-189가 경고한 고신뢰 오탐의 재현. **단 5건 전부 K=1 단발·seglen 0.24~1.02s**라 게이트(`K≥3 ∧ T≥1.0s ∧ seglen≥2.0s`)에 전부 걸리고(would_fire 0), 반대로 ytn1 참양성 run 2개는 통과 → 게이트 뒤에서 신호/잡음 분리됨. eng1·sbs1 0건. ytn1 25건 전부 참양성(정답 한국어→`I'm happy to be here` 의미보존 영어번역, LMR_ko 19.7%). **kor1~3 `--repeat 3` 확증 완료** — ko 누계 12세션 3756프로브에서 발동 23건(0.61%·kor2 b1/kor3 b1·b30이 3/3회차 반복하는 **결정론적** 오탐), 발동 시 seglen 최대 **1.02s**·`seglen≥2.0s` **0건**·최대 K=2 → **게이트 통과 0/23**. WER med kor1 21.6/kor2 24.1/kor3 35.8, LMR 9/9세션 0.0%. 정지 사유가 "표본 부족"에서 **"재현되는 고신뢰 오탐의 존재 자체"**(설계 수용 여부 판단)로 이동. 재개 시 ①완료 → 곧바로 ② 섀도우(`would_fire`) 가능. **en도 N=3 확증**(eng1 4세션 500프로브 발동 **0건**, WER med 3.8%) → 안 A 표본 전부 닫힘. **방향 분리 관측**: 15세션 통틀어 오탐 23건 전부 `locked=ko`·참양성 전부 `locked=en`(`locked=ko` 참양성 0·`locked=en` 오탐 0). 비대칭 문턱이 자연스러운 레버지만 실측 맞춤 사후조정=정지사유 그 자체라 미구현·기록만, 게다가 `locked=ko` 참양성 판별력 미측정이라 진짜 실패를 놓칠 위험 있음. 정본 = `docs/research/SOT_LANG_PROBE_STAGE0.md` §10 · 인계 = `docs/backlog/LANG_LOCK_STAGE0_HANDOFF.md` §4-0/§4-0-1 |
| Exp-194 | **E5** | auto | 2026-07-21 | frame_threshold 외 대안 병렬탐색(4클러스터) → D1(new_speaker refresh 직전 pre-boundary KO-슬라이스 재디코딩, `decode_tail_slice`) 1순위 도출·프로토타입(TDD 557pass) → **스크리닝에서 기각**. 삽입점을 new_speaker(지배경로)로 옮겨 `[TailSlice]` 실제 발동은 성공(bong1 12·ytn2 8·sbs1 2·ytn1 8회)했으나 목표(ytn2 "합의점에" 꼬리) 정작 그 경계에서 무로그 스킵 + 3/4파일 회귀 + bong1 어절중복·sbs1 유실/환각 신규발생 | 25.7→30.8%(악화) | 15.8→13.8%(단 핵심타깃 미달성) | 8.9→**23.8%**(2.7배 악화) | ❌ **기각**(repeat3 진행 없이 스크리닝 단계 기각) — 화자F1 다수 회귀(bong1 76.5→66.7·sbs1 100→66.7·ytn1 84.2→66.7)로 D1의 존재이유("전역비용0") 실측 미성립. 정확한 인과(왜 그 경계 스킵됐는지)는 진단로그 부족으로 미규명 — **재시도 시 3개 무로그 조기리턴 경로에 스킵사유 로그 추가 필수**. 코드레벨 시도 2회(Exp-193 flush·본건) 실패로 종료, **frame_threshold 배포레버(Exp-193) 최종 확정**. 워크트리 미커밋 보존 |
| Exp-193 | **E5** | auto | 2026-07-20 | frame_threshold를 KO→EN 경계 꼬리유실 제어 레버로 규명(ft=35 증폭 시도가 **완화**로 판명) + is_last flush 코드수정(`exp/boundary-tail-flush` 미머지 — 경계 前 KO 꼬리 강제 flush+`pending_langswitch_flush` 전파+`retract_protected` 철회보호+재진입가드, TDD 554pass) dead-end. 배포 "갱신하."류 mid-word 절단 = KO 꼬리 원천유실(미커밋→트림/스왑 폐기)의 심각형, GPU 배치크기로 심각도 결정 | 25.7→26.6%(ft35) | **15.8→11.8%(ft35·"합의점에" 꼬리 3/3복원)** | 8.9→11.3%(ft35) | 🟡 **frame_threshold 배포레버 채택**(`--frame-threshold 35` 스윕40-50, 사용자 승인) — 전역 기본값은 다화자 bong1 **화자F1 76.5→62.9 회귀(1순위)로 불가**. flush는 실질 미작동(전환 new_speaker 주도라 발동 ytn2 0/9, 걸려도 경계 얽힘 garbage)으로 **기각**. Case B 신규 0. frame_threshold 외 대안 탐색 진행 중 |
| Exp-190 | **E5** | auto+ko | 2026-07-19 | `SILENCE_HARD_SECS` 안전망 문법-조건부화 — `_gate_decide`·`_gate_intake_nondiar_silence`의 split_hard 분기에 `should_split_after_silence(...) is not False` 가드 추가. kor1 낭독체 긴 호흡 pause(d_eff=3.59s>cap2.0s)의 미종결 어절('2040년'/'국방환경')이 단어 중간에서 강제분할(Case B)되던 것 제거. 방향A(임계값↑)는 3.59>2.0 불변식으로 기각. (`exp/silence-hard-caseb-fix@1b34561` → master 머지 완료 `2a1391f`) | 26.9%(N=3) | 20.7%(N=3 재측정) | 14.9%(N=3) | ✅ 채택권고 — **Case B 0/6**(전 kor1 N=3 split_hard 미종결 0건 결정적·소모강요 diar-flip 0/6), kor1 auto med **18.7**(baseline23.0→개선)·ko 20.5, kor3 33.1(41.1→개선)·eng1 2.9(개선), kor2·ytn1 동등, 무회귀. bong1 max34.7/sbs1 max16.7 소폭초과=사전존재 웃음필러 변동(분할제거 수정이라 환각유발 불가). pytest 448 pass·ruff clean. **master 머지 완료(`2a1391f`)** |
| Exp-192 | **E5** | auto+ko | 2026-07-20 | 언어전환 경계 재조정(reconciliation) 계층 신규 — 파괴적 철회(pop)를 tombstone(복원가능) 마킹으로 대체(`ASRToken.retracted`), 신규 모듈 `boundary_reconcile.py`(ReconcileWindow: D1/D2/D3 마감·단일컷 원자성 대체/복원, 구역2 확대로 "미니스터"류 이중언어 중복 표적), 시간창+정규화 텍스트 결합 매칭 dedup(계측 중 순시간only 오탐 발견→수정), 언어전환 트림 keep 동적화(서두유실 대응, 상한5.0s). Exp-191(`exp/boundary-tail-dup`, ⓐⓑ휴면+ⓓON) 실패(표적지표 악화)를 대체하는 신규 설계, master(`2a1391f`)에서 재분기. (`exp/boundary-reconcile@8ff041b` → master 머지 완료 `b001e38`) | 30.2%(r1) | 16.3%(r1) | 10.1%(r1) | 🟡 스크리닝 유망·**확정(N=3)·held-out 미실시**(사용자 지시로 조기 중단, "이 버전 커밋 후 배포작업"). 직전(Exp-190) 스크리닝 대비 bong1+1.2pp(허용+3pp내)·ytn2 동일·sbs1 개선, ko 무회귀(kor3 편차는 재조정 dormant 확인된 노이즈). 표적지표(경계23곳 정상18/구언어꼬리3/기타2, dup 2건) 대폭개선("미니스터"류 유령라인·환각 소멸 확인). Case B 0(bong1 1건은 기존 silence-gate 계열로 분류, 무연관). pytest 540 pass·ruff 신규0. **CLAUDE.md §4 repeat=3 게이트 미통과 — 스크리닝 1회 방향신호일 뿐**이나 **master 머지됨(`b001e38`)**. ⚠️ 2026-07-21 정정: 한때 "Exp-190/192가 §2 음차 환각을 해소했다"고 기록됐으나 **오류** — Exp-190은 침묵 게이트(언어 무관), Exp-192는 언어 감지가 이미 옳다는 전제의 경계 정리 계층이라 둘 다 음차 대응책이 아니며, bong1 N=10 측정에서 음차가 5/10 재현된다(Exp-172 규명 이후 구현·측정된 대응 실험 없음) |
| Exp-189 | **E5** | auto | 2026-07-19 | kor1 auto모드 언어오검출(Exp-186) 근본원인 규명 — 공용 로그태그 `[ShortSilenceLangCheck]`에 속아 `_check_short_silence_language`(min_prob=0.90)를 의심했으나 실제 원인은 `new_speaker()`의 eager 언어감지(min_prob=0.85)가 Sortformer diar flip-flop 버스트(Exp-188과 동일 근원, 단일화자 kor1에서도 speaker 0↔1 반복 오귀속)마다 쿨다운 없이 재실행되며 점점 좁아지는 잡음창에서 고신뢰 오탐(en, p=0.95→1.00→0.99)을 재확인/고착시킨 것. `_EAGER_LANG_COOLDOWN_SECS=1.5s` 신규 도입(`exp/kor1-lang-misdetect`, master 병합 후 확정측정 완료, 미머지) | 23.0%(N=3 확정, max35.0) | 18.2%(N=3, max23.6) | 12.5%(N=3, max14.9) | ✅ 채택 확정(`--repeat 3`) — kor1 target median22.8/max**23.4%**(수정전 max47.4%, catastrophic 영어환각 소멸), en 고신뢰 오탐 0/3, NewSpeaker 0/3(버스트 미재현이나 사전 스크리닝 N=5에서 R5 58회 버스트 재현·오탐 0건 확인됨). auto/ko셋·held-out(ytn1/eng1) 전부 무회귀. Case B 1건(R1) 재확인됐으나 원인규명 결과 `SILENCE_HARD_SECS`(Exp-185) 안전망의 진짜 3.59s 침묵 반응으로 확정 — 이번 수정과 무관(병합 전 N=5에도 동일지점 재현), 후속 과제로 인계. pytest 437 passed·ruff clean |
| Exp-188 | **E5** | ko(+auto) | 2026-07-18 | kor1 Case B(단어중간분절) 근본원인 규명+수정 — Sortformer가 아주 짧은 diar 세그먼트에만 순간적으로 다른 화자를 오귀속하는 노이즈를 `_gate_decide`의 화자불일치 강제분할이 그대로 증폭(실측: 같은 침묵에서 `speakers=(1,1)/merge`→`(1,2)/split_grammar` 플립). `MIN_SPEAKER_ATTRIBUTION_SECS=0.5s`(승자 diar 세그먼트 길이 기준, ASR 텍스트 길이 아님) 신규 도입해 짧고 고립된 오귀속만 직전 화자 승계(`exp/kor1-caseb-fix`, 미머지) | kor1 18.4%(N=8)/kor2 17.2%(N=1)/kor3 29.8%(N=1) — 참고: auto 표준셋 bong1 31.7%(N=3)/ytn2 **15.8%**(N=3)/sbs1 10.7%(N=3) | 〃 | 〃 | ✅ 채택 — Case B 0/8(수정전 재현 1/3, kor2·kor3·ytn2·sbs1·bong1 전사 전수 0건), TDD 결정적 검증(`test_diar_short_segment_speaker_flip_does_not_force_case_b_split` red→green, 기존 27개 게이트 테스트 전부 통과), auto/ko 전 파일 무회귀(bong1 max 44.4%/F1 47.1%는 `[SpeakerAttribution]` 0/3 발동으로 이번 변경과 무관 확정 — 기존 웃음/필러 패턴 귀속) |
| Exp-187 | **E5** | auto(+ko) | 2026-07-18 | `finalize_trigger` 화자전환 라벨 손실 규명+수정 — `get_lines_diarization`의 TriggerAssign 분기우선순위(`is_silence()`가 화자비교보다 먼저 걸림)가 원인, `PuncSegment.speaker_boundary` 플래그로 `_gate_decide`의 기존 화자불일치 판정을 TriggerAssign까지 전달(`exp/speaker-change-trigger-loss@aec666e`, 미머지) | 29.0%(N=3, 전28.6%→후29.1% avg, 노이즈범위) | **15.8%**(N=3) | **11.9%**(N=3) | ✅ 채택 — 순수 라벨링 수정(물리적 세그먼트 경계·WER·F1 불변 설계, 전 파일 노이즈범위 무회귀 실측 확인). speaker_change 트리거 bong1 3회합 7→16(2.3배)로 대폭 증가(silence/punctuation 오귀속 해소), branch=else(fallback) 15개 로그 전수 0건 확정(부수관찰 실증), Case B 0건(15회차 전수), held-out ytn1·ko kor2/kor3 무회귀 |
| Exp-186 | **E5** | auto(kor1~3도 auto로 측정) | 2026-07-18 | 코드 변경 없음 — auto 모드 8파일(eng1 제외) N=3 진단 측정 + 정성분석 + 관찰용 디버그 로그 보강(`feat/debug-diagnostics-logging`, 커밋 67a58ad, 미머지) | 29.3%[27.8–32.6] | 15.8%[11.8–18.7] | 10.7%[10.1–11.9] | 📋 계측완료(채택/기각 비대상) — **①최우선** speaker_change 트리거 전 파일 저빈도(bong1 정답14회 vs 트리거1~4회, diarizer감지 26~27회) **②hard-fail** kor1 Case B 3건(Exp-185 SHS=1.2 이후에도 잔존, kor3는 해소 유지) **③** kor1 순수한국어→영어오검출 버그(WER상관) **④** kor2/kor3 화자F1 0/100진동=단일화자 참조 all-or-nothing 메트릭 아티팩트(Exp-185 다음가설2 해소, Sortformer무관) **⑤** auto vs `--lan ko`(Exp-185) 교차비교: auto 취약점은 kor1 국한(kor2/kor3는 auto가 동등·우수). 상세: docs/research/AUTO_MODE_KOREAN_PERF_ANALYSIS_20260718.md |
| Exp-185 | **E5** | auto(+ko/en) | 2026-07-18 | `SILENCE_HARD_SECS` 0.8→1.2(문법-조건부 침묵 게이트 안전망 문턱, CLI `--silence-hard-secs` 스윕 인프라 신설) | 31.4%(N=3, max34.4❌) | **17.2%**(N=3) | **10.7%**(N=3) | ✅ 채택 (사용자 승인) — kor3 Case B(단어중간분절) N=3 전회차 해소, auto 화자F1 71.4→85.0%(N=3) 개선, ko WER/F1 중립(회귀아님), held-out ytn1·eng1 개선. bong1 max 초과는 기존 웃음/필러 패턴 귀속(무관 판단) |
| Exp-184 | **E5** | ko(+en/auto) | 2026-07-17 | 세션별 언어 고정(auto/ko/en): `?language=`/`--lan` 세션 cfg 얕은사본(`_resolve_session_cfg`)로 코드스위칭 게이트 6곳 세션언어 추종 + 재감지 로직 auto가드 5곳(new_speaker·ForeignLang·ScriptMismatch·긴침묵·periodic) + online_factory 프록시분기 + basic_server 검증·config필드 + 내장UI 드롭다운, **master 머지 `ac17b00`** | (auto 표준셋 무영향·게이트 inert) | 〃 | 〃 | ✅ 채택 (사용자 승인·opt-in, §3.2 기반기능). **ko 스크리닝**: kor1 20.5%/kor2 18.8%/kor3 38.4%(평균 25.9% vs auto 29.5%, kor3 −12.6%p). eng1(en) 8.6% vs auto 1.9%(스크리닝 편차 추정). auto 구조적 무회귀·pytest 425 pass. Case B는 silence-churn 기존이슈(고정 무관). **epoch 미bump**(게이트 auto inert). --repeat 3 후속 |
| Exp-183 | **E5** | — | 2026-07-17 | Segment 안정 id 스키마 필드 추가(`timed_objects.py` `to_dict`) + 내장UI 키를 id로 전환 + 배포 React 문서계약 정정(API_SPEC/SCHEMA_CHANGES/FRONTEND_HANDOFF — 복합키 지침 폐기→전체교체/id 누적), **master 머지** | (표준셋 영향 미미) | (표준셋 영향 미미) | (표준셋 영향 미미) | ✅ 채택 (사용자 승인, additive 스키마·백엔드 STT 무변경·pytest 411 pass·ruff pass) — id=round(start,3) 세션상대초, end 성장·재개방·같은초 충돌 무관 안정키. **bong1 다화자 무회귀 실증**(WER 23.3%·화자F1 58.5%, start단독키 Exp-182의 같은초충돌 잠재리스크 해소). 부수 규명: `_DEFAULT_RETENTION_SECONDS=inf`(리텐션 무제한). epoch 미bump |
| Exp-182 | **E5** | — | 2026-07-17 | 프런트 실시간 전사 중복표시 2종 수정(프런트 전용, `live_transcription.js`) — ① `finalizedHistory` 키를 start 단독으로(최종화면 growing-prefix 중복) ② 진행중 줄과 같은 start의 stale 확정줄 억제(실시간 진행중 화면 중복), **master 머지 `36fdbaf`** | (표준셋 영향 미미) | (표준셋 영향 미미) | (표준셋 영향 미미) | ✅ 채택 (사용자 승인, 프런트 렌더 전용·백엔드 STT 무변경·pytest 411 pass) — kor2 WER 95.1→25.0%(①만, -70pt)·kor3 57.0→30.5%: **과거 kor1~3 WER이 이 UI 렌더 아티팩트로 과대평가됐음 실증**. ★Exp-180 "kor2 회귀"의 핵심 근거였던 절대 WER도 이 아티팩트 낀 값 → 재검토 대상(OFF/ON 동일 영향이라 상대 비교·로그결론은 유효). ② 실시간 중복은 라이브 육안검증 필요(경로 C 최종DOM 검증불가); epoch 미bump |
| Exp-181 | **E5** | — | 2026-07-17 | kor2/kor3 silence-churn 근본원인 규명(계측·코드무변경) — growing-prefix 중복의 정체는 프런트 `live_transcription.js:375` `finalizedHistory` Map이 `start\|end\|speaker`로 키를 잡아, Exp-176 문법게이트의 사후 경계재조립(같은 세그먼트 `end`가 커짐)을 매번 "새 줄"로 오인 누적하는 것 확정 | — | — | — | 📋 계측완료 (채택/기각 비대상) — Exp-179 "Exp-177 Bug1 계열" 가설은 기각(그건 이미 수정된 타임스탬프 오프셋 버그, 이번 건 세그먼트 경계 재조립+프런트 키 충돌); 경로 C WER이 이 프런트 아티팩트로 과대평가됐을 가능성(다음 가설 1번 검증 대기); kor3은 이번 회차 재현 안 됨(간헐적) |
| Exp-180 | **E5** | — | 2026-07-17 | ScriptAnchorRedetect 약어 철자낭독 오발동 가드(P1, Exp-179 후속) — 약어형 Latin 토큰(길이≤2 또는 전대문자≤6자) streak 중립 스킵, `exp/scriptanchor-acronym-guard@747e47f`, **master 머지 `cfb0387`** | 38.1%(+2.4pt) | **17.2%**(-3.4pt) | 10.7%(+0.6pt) | ✅ 채택 (사용자 승인, 정량게이트 부분미충족이나 §4 기반기능 질의절차) — 표적 kor2 오발동 3라운드 전부 0건(로그확정), Case B 0건; **부작용**: kor2 WER +29.2pt(별개 silence-churn 결함 세션변동 추정, 확증 안 됨)·sbs1 화자F1 worst -33.4pt(저표본, 원인미규명)·kor1 med +11.1pt; kor3 -15.2pt·ytn1 개선; kor2 silence-churn을 다음 조사로 착수 |
| Exp-179 | **E5** | — | 2026-07-15 | 세션 초입 언어 프로브(2.0s 누적 시 커밋 없이 감지, p≥0.85만 적용) — Exp-178 콜드스타트 데드락 해소, `exp/session-start-lang-probe@b842c6f` 워크트리(미머지) | 34.4%(N=3, 발동0=경로동일) | 17.7%(N=3, 발동0=경로동일) | 10.1%(N=3, 발동3/3·max15.5<게이트) | ✅ 채택 (사용자 승인, **master 머지 `27f3f3c`**; epoch 미bump — 미발동 시 완전 수동, 디코더 파라미터 무영향) — 표적 kor1 med 51.5→44.4·max 62.0→46.2, 발동 전수 정당(ko×8·en×1·오적용0), held-out ytn1 12.3/eng1 2.9 미회귀, kinno max 72.0→39.4(단 발동0이라 귀속불가·미재현 관찰) — 표적 kor1 med 51.5→27.8·max 62.0→34.5, 서두 2문장 복구 2/2, 발동 6건 전수 정답언어(ko×4·en×2, p 0.95~0.999), 미발동 파일 코드경로상 중립; **신규 규명**: ① kor2 폭주(70~109%) 주범 = Exp-175 ScriptAnchorRedetect **철자낭독("GP·GOP") 사각지대** ko→en 오전환+9.7~12s 트림 폐기+중복 재확정(OFF/ON 공통·프로브 무관) ② long-silence 리셋이 detected_language 초기화 → 중간 데드락 재진입(프로브 2차 발동으로 커버 실증); 화자F1 ytn2 56/sbs1 40(N=1·미발동 런)은 채택확정 N=3서 확인 |
| Exp-178 | **E5** | — | 2026-07-15 | 한국어 단독 데이터 kor1~3 신규 측정(코드 무변경) — 배포 한국어 성능저하 dev 재현, 정답 canonical 복사 생성 | — | 13.8%(비교군 N=1) | 14.9%(비교군 N=1) | 📋 계측완료 (채택/기각 비대상) — kor1 62.0/kor2 47.9/kor3 49.7%로 붕괴 vs 비교군(ytn2·eng1·sbs1) 전부 베이스라인 수준=하니스 무결·한국어 단독 낭독체 고유 실패 확정; 원인 3갈래 귀속: ① 서두유실 3/3=초입 영어환각("we have"/"Thank"/"I'm sorry") QG streak refresh 버퍼폐기(Exp-177 Type B — 배포 "영어 전사·Thank you" 증상의 정체) ② kor1 중간 ~44s 통유실=stall recovery 5연발 웨지(**신규 모드 후보**, last_end 고착) ③ kor3 **Case B 3건 hard-fail**("통합."⏎"하고"류 — Exp-176 SilenceGate 머지 상태에서도 낭독체 0.8s+ 호흡 pause가 `SILENCE_HARD_SECS` 안전망 트리거로 문법판정 우회); 부기: 중복확정 churn(kor2 2건×2회·kor3 1건×3회)+한국어 종결어미형 필러 환각; kor1~3 테스트셋 편입=사용자 결정 대기 |
| Exp-177 | **E5** | — | 2026-07-13 | 배포 증상 재현 계측(코드 무변경) — 필러 삼킴 Type A/B 분류, Type B=경계 QG streak refresh 버퍼폐기 신규 규명(12run 25회·kinno 발생수-WER 강상관), kinno 최초 N=3(med 30.5/max **72.0%**), goal 신설 `docs/goal_prompt/GOAL_BOUNDARY_QG_PRESERVE.md` | 34.1%(max36.9) | 21.2%(max24.1) | 13.1%(max13.7) | 📋 계측완료 (채택/기각 비대상) — 배포 두 증상(필러 삼킴·중간 유실) dev 완전 재현=반입 문제 배제; Type A=Exp-169 사각지대 재확인(kinno R3 "Thank you" 663줄 vs AnchorRepeat 4회 발동)+ScriptMismatch 0회, Type B=eager 실패(p=0.54)→폴백 ko p=0.91 성공에도 QG 3연속→`refresh_segment(complete=True)` 서두 오디오 1.53s 비가역 폐기→"예수님과 관련 네, 감사합니다" 환각 대체(Exp-154/173 "부당드롭≈0~1%"는 텍스트 억제만—오디오 폐기는 스코프 밖이었음); regime v2 2-F1 최초 N=3 기록(화자F1 bong1 51.3/ytn2 76.2/sbs1 100) |
| Exp-176 | **E5** | — | 2026-07-12 | 문법-조건부 침묵 경계 게이트 — Case B(단어중간분절) 방지, `SILENCE_HARD_SECS=0.8` 안전망+decide-late+이원유예+메모+hard_boundary스탬프(`tokens_alignment.py`), **master 머지(`c1f5884`)** | 29.3%(max38.7, OFF28.7/36.9) | 16.7%(max29.6🟡, OFF19.7/20.2) | **11.9%**(max13.1, OFF20.2/30.4 대폭개선) | ✅ 채택 (master 머지 `c1f5884` — 이 행의 "미머지" 서술은 stale이었음, Exp-178 세션서 정정) — 화자분리F1 3파일 전부 worst-case 개선(sbs1 50%→100%), Case B 게이트 통제범위 N=3 전회차 재발 0건, held-out ytn1 10.4%(21.5%→대폭개선)·eng1 3.8% 양호, 영어대문자 규칙 회귀없음; ytn2 WER max만 OFF 대비 +9.4pt(참고게이트 34.5%는 이내, 원인은 토큰반복 환각 추정이나 경계로직 무관 여부 미확증) — 사용자 질의 대기 |
| Exp-175 | **E5** | — | 2026-07-09 | 스크립트-앵커 재감지 게이트 — 무음·화자전환 없는 연속 코드스위칭에서 방출 스크립트 반전(N=3단어/T=1.0s) 시 재감지·전환(`backend.py` 신규 게이트), **master 머지(`c3302a2`)** | **30.8%**(max36.0, -6.7pp) | 17.2%(max18.2) | 16.7%(max17.9→**격리 N=3 10.1/max12.5** — 측정순서 효과 규명) | ✅ 채택 (사용자 승인 — GOAL §4 게이트 4항 전부 충족: 발동 전수 정당·오탐 0, Exp-172 확정 유실 "You don't understand" 게이트 직접 복구, held-out ytn1 16.0/eng1 5.7 미회귀; 사용자 질의 후 **짝지음 A/B 18런 추가 검증**: 브랜치 발동 0회로 WER 완전 중립 확정(median 합계 +1.4pp=노이즈, 이상치 48.2%는 Exp-168 전례의 기존 sbs1 stall 모드·38.4%는 필러/웃음 변동으로 게이트 무관), sbs1 N=3 상승은 배치 후미 측정순서 효과 분리 실증; N스윕 N=2 오트리거·N=4 미발동→N=3 유지; **epoch 미bump**(E5 유지 — 언어전환 경계 서브시스템 내 트리거 추가, 미발동 시 완전 수동) |
| Exp-174 | **E5** | — | 2026-07-08 | 언어전환 경계 철회 하한을 재디코딩 창 시작(`retract_floor`)으로 정정 — 화자전환 트림 후 재디코딩 불가능한 서두 토큰 보존, master 머지(`500d175`) | 37.5%(max39.3❌, +9.4pp) | **15.8%**(-2.9pp) | **10.7%**(-2.4pp) | ✅ 채택 (사용자 승인 — bong1 "You don't understand" 서두유실 3/3 확정복구, ytn2·sbs1 median 개선, held-out ytn1 10.4%/eng1 5.7% 양호; bong1 max 초과는 로그 인과관계 대조상 필러/웃음 기존 실패모드(Exp-171 R2 43.5%와 정성일치, 제 max가 오히려 더 낮음)로 무관 확인, 전체 pytest 4건 실패는 plain master 단독실행도 동일해 무관 확정) |
| Exp-173 | **E5** | — | 2026-07-08 | VAD `min_silence_duration_ms` 100→200ms — 단어 내부 미세정적 오분할 방지 시도(실제 개선기전=반복환각 감소), master 머지(`01e36a0`) | 38.4%→**36.9%**(max46.8→37.2 대폭개선) | 16.7%→**15.8%**(max동률18.7) | 13.1%→12.5%(max14.3→17.3 소폭회귀·비재현) | ✅ 채택 (①ytn2·bong1 median+worst-case 개선 ②held-out ytn1 -9.8pp/eng1 -2.9pp 일반화 확인 ③sbs1 소폭 회귀는 비재현 노이즈; 사용자 승인) |
| Exp-172 | **E5** | — | 2026-07-08 | Stage 0 계측(GOAL_CODESWITCH_BOUNDARY, 코드 무변경) — `--trace-tokens` N=1로 3증상 유실 경로 귀속 | 32.9%(N=1) | 16.7%(N=1) | 14.3%(N=1) | 📋 계측완료 (채택/기각 비대상) — ① 서두유실 범인=[Retract] 철회 후 재디코딩 미복구(bong1 "You don't understand" 4단어 확정), ③ 중간유실 범인=[QualityGate] 정당단어 억제(ytn2 "verified" 확정·**E4 "부당드롭 0%" 결론 turbo에서 반증**)+held/UTF-8 손상(sbs1)+세션초입 buffer(3파일 공통); CrossBatch/AnchorRepeat 기여 미미로 후순위 강등; ①′ 신유형=locked-lang 음차 환각(스크립트 감지 사각지대) 실측; Stage 1 임계 실측근거 N=3단어/T≈1.0s |
| Exp-171 | **E5** | — | 2026-07-08 | 언어전환 경계 오언어 토큰 철회(2구역 규칙)+화자전환 서두유실 수정(`refresh_segment keep_secs`+앵커재계산), master 머지(`a14d028`) | 28.1%(max43.5❌) | **18.7%**(max18.7, -10.9pp) | 13.1%(max15.5) | ✅ 채택 (사용자 승인 — 원 결함(오번역잔존·접착·서두유실) 전회차 해소, ytn2 §3.8 최우선 대폭개선, held-out 양호; bong1 max 초과는 R2 1회 이상치·기존 필러/웃음 실패모드와 정성적 일치로 회차변동 판단, `--trace-tokens` 재현조사는 후속과제) |
| Exp-170 | **E5** | — | 2026-07-07 | 온점 형태소 분할 — 한국어 종결어미·영어 약어 판별로 온점을 독립 문장경계로 승격(`sentence_boundary.py` 신설·`PuncSegment.punct_boundary`·`_punct_split_here`, `4b193ea`), master 머지 | 30.2%(max36.0) | 29.6% | 14.3% | ✅ 채택 (사용자 승인 — 목표=문장단위 확정 §3.3). WER median≈baseline·**~WER중립(출력계층 전용, 단어 불변)**·정성정확(형태소분할 니다/했습니다·거짓마침표 미분할 "주한미군/올렸/관련"·약어가드 "Dr.Bong")·ytn2 F1+16.7pp. F1 하락(sbs1 13.3/bong1 44.9)은 정답이 **화자전환(문단) 경계 기준**이라 문장분할을 penalize하는 **지표한계**(§3.3 온점 2순위 metric 미구현) — sbs1 R3 F1=0.0%인데 전사는 14개 정문이 증거. bong1 WER max 36.0(>게이트30.5)은 **웃음 필러환각 변동**(디코더, 출력계층 기능과 무관 확인). **epoch 미bump** — 출력계층 전용이라 디코더 파라미터(beam/CRT/PLC) 트레이드오프 불변, E5 결론 유효 |
| Exp-169 | **E5** | — | 2026-07-07 | CASE3 환각폭주 — script-agnostic 앵커 반복(필러storm) 게이트 신설(`_find_anchor_repeat_storm`, `dc0dc35`), master 미머지·사용자확인대기 | 35.3% | 26.1% | 13.7% | 🟡 조건부채택권고 (§5 3조건 클린통과 + storm 최대반복 4→3·ytn2"김정은"storm 소멸 달성; 단 Req-2 두경계 재검증 결과 3/3 아닌 2/3씩(각 1회 catastrophic swallow 재현) — 게이트 미발동 구간에서 발생해 이번 변경과 인과무관 확정, 가변변주구가 gap-tolerant 클러스터링을 3+2로 쪼개는 사각지대 잔존; 사용자 확인 후 머지여부 결정) |
| Exp-168 | **E5** | — | 2026-07-07 | CASE2 코드스위칭 서두유실 — 전환경계 필러환각 해소(언어-스크립트 불일치 게이트 `8aeb5a2` + eager 언어감지 since_offset `0fed0d5`), master 머지 | 27.8% | **19.7%**(43.8%→대폭개선) | 16.7%(max48.8%❌) | ✅ 채택 (ytn2 두 경계 필러폭주 3/3·3/3 해소; sbs1 max초과는 화자전환0회라 이번 코드경로 미호출 확인 + 기존 저빈도 stall현상(Exp-167서도 0/3)임을 실증, 사용자 확인 후 채택; ytn1 held-out도 개선) |
| Exp-167 | **E5** | — | 2026-07-07 | CASE1 문장꼬리분리 4대경로 완료 — 온점분할 갭기반 (c)분기 완전제거(FIX1, `e6ae496`; Direction A `dd33fe7`는 갭측정 혼동으로 기각), master 머지 | 28.7% | 36.0%(별개이슈) | 14.3% | ✅ 채택 (bong1 타겟패턴 "자빠졌/는데" 3/3 확정해결; sbs1 "것으로/보입니다" 잔존 1/3은 CASE1 버그 아닌 Silence 0.4s 문턱 경계 정책 이슈로 재분류 실증; ytn2 WER악화는 기존 "Thank you" 필러환각(CASE3 대상)과 무관 확인) |
| Exp-166 | **E5** | — | 2026-07-06 | LANG_SWITCH_KEEP_SECS 스윕(3.5/4.5) `--lang-switch-keep-secs` CLI, master 미머지 | 31.1/28.4% | **40.9/34.5%** | 15.5/**18.5%** | ❌ 기각 (N=1: ytn2 두 값 모두 baseline median 위·서두 유실 미완화(정성 잔존/악화), sbs1 4.5 게이트 초과; ytn2 지배오류=필러스톰·방송환각으로 keep_secs 무관) |
| Exp-165 | **E5** | — | 2026-07-06 | tail-only no_speech shadow probe(tail-slice 재조합 계측), master 미머지 | — (probe 로깅전용·WER 해석불가) | — | — | ❌ 기각·폐기확정 ((b)분리불가: tail 513샘플+고정밀 290샘플 전부 no_speech=0.000000, ns_logit≈-10 고착 = turbo no_speech 헤드 degenerate; Exp-164 "버퍼오염" 진단 정정 = tail도 0; Layer 3b no_speech 계열 폐기, P3 스킵) |
| Exp-164 | **E5** | — | 2026-07-06 | 비음성 게이팅(no_speech 계측+VAC 임계값 스윕 0.4), master 미머지 | **34.4% (max초과 ❌)** | 30.0% | 11.9% | ❌ 기각 (no_speech는 롤링버퍼 전체판정이라 구조적 무효(1221샘플 전부 0.000) — 스윕 스킵; VAC=0.4는 게이트 초과 + 웃음/필러 전사 그대로 잔존, VAC가 음성에너지만 판정해 개념적으로 부적합) |
| Exp-163 | **E5** | — | 2026-07-06 | cross-batch 필러 반복 필터(v1 연속 n-gram→v2 윈도우-앵커), master 미머지 | 26.3% (필터0회·분산) | **34.0% (max46.8 ❌)** | 16.1% | ❌ 기각 (ytn2 max catastrophic; 후처리 필터가 디코더 필러 재생성 못 막고 storm-refresh가 ytn2 정렬 교란해 악화; 원천 비음성게이팅으로 피벗) |
| Exp-162 | **E5** | — | 2026-07-06 | P3 재검증(beam 1/3, CRT 2.5/2.8), 코드변경 없음 — 스크리닝만 | — | — | — | ➖ 변경없음 채택 (beam=2·CRT=3.0 재확인, beam=1/3·CRT=2.5는 게이트 초과로 스크리닝 단계 기각, CRT=2.8은 baseline과 무구분) |
| Exp-161 | **E5** | — | 2026-07-06 | audio_max_len 기본값 30.0→15.0 — sbs1 실시간 lag(41s) 해결 | 30.5% (동일, stdev 3.0→0.7 안정화) | **28.1%** (30.0%에서 추가개선) | **14.9%** (23.2%에서 대폭개선) | ✅ 채택 (3파일 모두 max·median·stdev 전부 개선 — 가장 명확한 채택; lag 41s→2s대) |
| Exp-160 | **E5** | — | 2026-07-05 | PLC 기본값 4.0→None(비활성) — ytn2 스퓨리어스 전환→방송클로징 환각 근본원인 규명 | 30.5% (28.1%에서 소폭악화) | **30.0%** (41.9%에서 -11.9pp 개선) | 23.2% (16.1%에서 악화, max는 개선) | ✅ 채택 (①max 3파일 모두 미회귀 ②ytn2 median·max 확실개선; PLC=2.0은 N=3서 재현실패해 기각) |
| Exp-159 | **E5** | — | 2026-07-05 | held-out(ytn1/eng1) 확정 측정, 코드변경 없음 — P0 완료 | — | — | — | ℹ️ baseline 확정 (ytn1 33.1%로 ytn2보다 8.8pp 양호 → ytn2 회귀는 파일고유 난이도 쪽; eng1 3.8%로 영어 회귀 없음; 게이트 max 변경 없음) |
| Exp-158 | **E5** | — | 2026-07-05 | model_dir 배선버그 수정 + no_grad stall 수정 — turbo 기질 전환 [E4→E5] | 28.1% (base 대비 -8.2) | 41.9% (base 대비 +16.3 ❌) | 16.1% (base 대비 -4.1) | ✅ 채택 (correctness 버그 — WER 게이트 무관 필수, 폐쇄망 배포 블로커였음; ytn2 회귀는 코드스위칭 스캐폴딩 base종속 추정, 별도 재조사 필요; held-out 미측정) |

---

> **신규 실험 기록 위치**: Exp-159+ 전체 서술은 **[EXPERIMENTS_LOG.md](EXPERIMENTS_LOG.md)** 에 추가하고(작성 형식·전사 정성분석 가이드는 LOG 상단 + `/log-experiment`), **이 STATE 파일에는 위 빠른참조 표에 1행만** 추가한다(Epoch 열 포함). 확정 결론이 바뀌면 "이월 핵심사실"도 갱신. 구조 변경이 master에 머지되면 **epoch 마커를 올리고** 이전 epoch 파라미터 결론에 `[E?·재검증]` 부여. **[regime v2]** 2-F1 metric 코드 착지 후 신 Exp는 LOG에 **화자분리 F1 + 문장분리 F1을 따로** 기록한다(구 단일 F1과 비교 불가 명시; 우선순위 화자분리 F1 > WER > 문장분리 F1 — [docs/TRANSCRIPTION_REQUIREMENTS.md](docs/TRANSCRIPTION_REQUIREMENTS.md)). 빠른참조 표는 WER med 중심 유지, 화자분리 F1 열 추가 여부는 구현 세션에서 결정.

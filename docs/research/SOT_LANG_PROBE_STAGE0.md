# [SotLangProbe] 언어잠금 실패 예측력 측정 — Stage 0 go/no-go

> 2026-07-21, `exp/lang-lock-stage0`. **읽기 전용 로그 분석**(코드 무변경).
> 대상: 매 배치 SOT 위치 사후분포 계측(`[SotLangProbe]`)이 **언어잠금 실패를 예방 가능한 시점에
> 예측하는가**. 자동화 스크립트 = `scripts/analyze_sot_lang_probe.py`.
> 데이터 = `.omc/server_logs/server_bong1_C_R1_20260721_212212.log`(프로브 479개) +
> `server_ytn2_C_R1_20260721_212532.log`(프로브 244개). **각 파일 1회차뿐**(§7 한계).

## 0. 질문과 결론(TL;DR)

**질문**: 선행 연구 [TRANSLIT_LANGID_PROBE.md](TRANSLIT_LANGID_PROBE.md)는 lang_id 를 재감지
트리거로 쓰는 설계에 **NO-GO**를 냈다. 핵심 근거는 선제성 부재(0/11)였고, 그 한계로
"기존 로그엔 코드가 lang_id 를 *호출한* 지점의 성긴 프로브만 있다"는 계측 공백을 명시하며
§6에서 **매 배치 연속 프로브**를 후속 과제로 제안했다. `[SotLangProbe]`가 바로 그 계측이다.
그 연속 궤적에서 ① 표적/대조를 가르는 문턱이 존재하는가 ② 검출이 실패보다 이르거나 동시인가
③ 그 문턱에서 오탐이 희소한가?

**결론: GO — 단, "지속 불일치 경보" 용법에 한정한다.**
- **문턱 존재**: 판별 통계량 `p_opp`(잠긴 언어의 *반대쪽* 재정규화 확률)에서 표적 중앙값
  **0.991~0.999** vs 대조 중앙값 **0.0001~0.0002** — **3~4 자릿수 분리**. bong1에서 τ=0.97 이면
  표적 28/37 검출 · 대조 **0/396**(완전 분리).
- **선제성 3/3 충족**: 이벤트 A = 첫 오방출과 **동시**(원시 p_ko 기준으론 **0.80s 앞**, 그리고
  잘못된 잠금이 *적용되기 전*), 이벤트 B = **동시**(0.00s), 신규 발굴 이벤트 C = **2.88s 앞**
  (9배치 연속 발화, 그 사이 방출 0건).
- **오탐 0건**: 두 파일 합계 **59회 발동 전부**가 실제 실패(54) 또는 실제 언어전환 경계(5)에
  귀속. 정상 전사 구간에서 발동한 건은 **한 건도 없다**.
- **선행 연구가 왜 뒤집혔는가**: 결론이 바뀐 것이 아니라 **계측 대상이 바뀌었다**. 선행 연구가
  본 것은 *호출 지점의 성긴 프로브*였고(그 결론은 여전히 유효), 여기서 본 것은 *매 배치 연속
  궤적*이다. §4의 "인코더 공유" 기전도 반증되지 않았다 — 오히려 §5에서 그 기전이 **왜 이번엔
  유리하게 작동하는지**를 설명한다.

**단, 확장 금지 범위가 있다**: 프로브는 **오디오 버퍼의 내용**을 읽는다. 전환 경계에서는 버퍼가
아직 *이전* 언어를 담고 있어 프로브가 **직전 잠금에 동조**한다(bong1에서 3회 관측, p_en 0.995~0.9997).
따라서 "**전환을 프로브로 승인**하는" 규칙(switch-confirmation)은 **이 데이터가 반증**한다 —
정당한 한국어 전환 2건을 차단했을 것이다. 지지되는 용법은 "**잠금 유지 중 지속 불일치 →
재감지 강제**"뿐이다(§6).

## 1. 방법 — 판별 통계량과 창 정의

**`p_opp` = 현재 잠긴 언어의 반대쪽 재정규화 확률**(locked=en → `p_ko`, locked=ko → `p_en`).
원시 `p_ko`를 창끼리 비교하면 "어느 언어가 잠겨 있었나"에 교란되므로 `p_opp`로 정규화했다.
`locked=None` 프로브는 `p_opp` 미정의라 그 통계에선 제외하되, 원시 `p_ko`/`p_en`은 궤적에 함께
표시한다(잠금 *적용 전* 경보 가능성을 보기 위해).

**표적 창**(과제가 지정한 언어잠금 실패 2건 + 분석 중 발굴한 1건):

| 이벤트 | 파일 | t_abs | 정답 | 전사 |
|---|---|---|---|---|
| A | bong1 R1 | 14.3–17.5s | 누가 주인공일까 이런 생각을 제가 제일 많이 했어요 | `is the one who is the main I think that's what I've done` |
| B | bong1 R1 | 112.2–119.3s | 죄송합니다 형님. 들고서 메타포리칼하다고 하잖아요. | `Sorry, my name metaphorical as a metaphorical thing, so.` |
| **C** | ytn2 R1 | 37.0–44.0s | 이런 목표들을 달성하기 위해서 우리는 UN안보리 결의를 집행하는 것과 관련해 저희 변함 없는 입장을 고수하고 있습니다. | `Thank you very much. Thank you very much. for your attention.` |

> **이벤트 C는 과제가 준 목록에 없었다 — 프로브 발동 지점을 정답과 대조하다 발굴했다.**
> ytn2 정답 문장 하나가 **통째로 소실**되고 영어 필러 환각으로 대체된 케이스로, 이 회차
> ytn2 WER의 단일 최대 기여 구간이다. 신호가 *사전에 지목되지 않은* 실패를 잡아낸 것은
> 판별력의 독립 증거다.

**대조 창** = 표적 창 ±3.0s 를 제외한 전 구간(bong1 396개 · ytn2 196개 `p_opp` 표본).
추가로 **음성 대조군**으로 bong1 `플라스틱 말랑말랑` 구간(t 88–93s)을 따로 집계했다 —
과거 10회 중 5회 붕괴했으나 **이번 회차엔 정상 한국어로 전사된** 구간이다.

## 2. 신호별 판별력 (정량)

### bong1 R1 (표적 A+B n=37 · 대조 n=396~415)

| 신호 | 표적 med | 대조 med | 대조 q90 | 대조 max | 분리 문턱 |
|---|---|---|---|---|---|
| **`p_opp`** | **0.9914** | **0.0001** | 0.0589 | 0.9538 | **τ=0.97 → 표적 28/37 · 대조 0/396** |
| `resid` | 0.5243 | 0.5120 | 0.5729 | 0.9702 | **없음**(§4) |
| `H_lang` | 0.1036 | 0.0123 | 0.7004 | 2.8691 | **없음** |
| `p_abs_ko` | 0.4728 | 0.0002 | 0.4964 | 0.5278 | 없음(τ≥0.5에서 표적 8.1% · 대조 9.2%) |
| `p_abs_en` | 0.0043 | 0.4757 | 0.4954 | 0.5065 | `p_opp`의 거울상 — 독립 정보 없음 |
| `p_nospeech` | 0.0000 | 0.0000 | 0.0000 | 0.0000 | **전 배치 0** |
| `p_translate` | 0.0000 | 0.0000 | 0.0000 | **0.0000** | **전 배치 0 → S3 반증** |

`p_opp` 문턱 스윕(표적 검출 / 대조 발동):

```
τ>=0.90  표적 30/37 (81.1%)  대조 1/396 (0.3%)
τ>=0.95  표적 29/37 (78.4%)  대조 1/396 (0.3%)
τ>=0.97  표적 28/37 (75.7%)  대조 0/396 (0.0%)   ← 완전 분리
τ>=0.99  표적 19/37 (51.4%)  대조 0/396 (0.0%)
```

**음성 대조군(플라스틱 t88–93, 정상 한국어 전사)**: `p_opp` n=14 med **0.0579** max **0.1433** —
표적 분포와 전혀 겹치지 않는다. "같은 오디오·같은 회차인데 실패하지 않은 구간"에서 신호가
침묵한다는 것은, 신호가 *오디오 난이도*가 아니라 *잠금 오류*에 반응한다는 증거다.

### ytn2 R1 (표적 C n=24 · 대조 n=196~201)

| 신호 | 표적 med | 대조 med | 대조 q90 | 분리 문턱 |
|---|---|---|---|---|
| **`p_opp`** | **0.9993** | **0.0002** | 0.0771 | **τ=0.99 → 표적 22/24 · 대조 1/196** |
| `resid` | 0.5454 | 0.5090 | 0.5823 | **없음 · 방향 역전**(τ≥0.7에서 표적 0/24 · 대조 7/201) |
| `H_lang` | 0.0149 | 0.0079 | 0.4214 | **없음 · 방향 역전**(τ≥0.9에서 표적 0/24 · 대조 7/201) |
| `p_translate` | 0.0000 | 0.0000 | 0.0000 | **전 배치 0 → S3 반증** |

> **S3(translate 태스크 누출) 반증 확정**: 두 로그 723개 프로브 전부에서
> `p_translate=0.000000`, 세션 요약도 `high_translate=0 max_p_translate=0.0000`.
> 디코더가 한국어를 영어로 "번역"하듯 방출할 때조차 **task 위치는 `transcribe`에 100% 고정**
> (`p_transcribe` 0.99995~0.99999)이다. 즉 이 실패는 task 토큰 누출이 아니라 **언어 토큰 오지정**이다.
> `p_translate`·`p_nospeech`는 신호 후보에서 **제외**한다.

**`resid`·`H_lang`은 판별력이 없다**(ytn2에선 부호까지 뒤집힌다). 유일하게 살아남은 신호는
`p_opp` 하나뿐이며, `p_abs_*`는 그 거울상이라 독립 정보를 주지 않는다.

## 3. 선제성 (이 실험의 존재 이유)

선행 연구가 NO-GO를 낸 정확한 사유가 여기다 — "실패가 이미 커밋된 뒤 늦게 오는 신호는 무가치".

### 이벤트 A (bong1 t=14.34 첫 오방출 `is the one`)

```
b17  t=11.27 lk=en   p_opp=0.0016  ko=0.0016   Output 'think'      ← 정상 영어
b18  t=11.27 lk=en   p_opp=0.0016  ko=0.0016
b19  t=13.06 lk=None  p_opp= n/a   ko=0.3187                        ← 잠금 리셋, 버퍼 0.24s
b20  t=13.30 lk=None  p_opp= n/a   ko=0.8775
b21  t=13.54 lk=None  p_opp= n/a   ko=0.9985   → LangSwitch 적용 en(prev=None)  ★
b22  t=14.34 lk=en    p_opp=0.9994 ko=0.9994   Output 'is the one' ← 실패 커밋
```

- `p_opp` 기준 첫 돌파 = b22, 첫 오방출과 **동시(+0.00s)**.
- **그러나 원시 `p_ko`는 b21(t=13.54)에서 이미 0.9985**이고, 이는 잘못된 `en` 잠금이
  **적용되기 직전**이며 첫 오방출보다 **0.80s 이르다**. 개입 지점(잠금 결정)에서 보면 **선제**다.
- 직전 정상 영어 구간(b7–b18)의 `p_ko`는 0.0005~0.0024 — 같은 잠금(en) 아래 **3자릿수 차이**.

### 이벤트 B (bong1 t=112.23 첫 오방출 `- Sorry`)

```
b318 t=111.75 lk=ko  p_ko=0.5590 p_en=0.4410  Output '형'
     → LangSwitch 적용 en (prev=ko, switch=True)   ★ 이 전환이 실패의 원인
b319 t=112.23 lk=en  p_opp=0.9832                Output '- Sorry'  ← 실패 커밋
```

- `p_opp` 첫 돌파 = b319, 첫 오방출과 **동시(+0.00s)**. **엄밀히는 사전 경보가 아니다.**
- 전환 *직전* 프로브(b318)는 `p_en=0.4410` — 애매값이라 `p_opp≥0.9` 규칙으론 잡히지 않는다.
- 즉 B는 **동시 검출**이며, b319 이후 22배치(t 112.2→119.7)에 걸친 오방출은 막을 수 있었으나
  **첫 방출 자체는 막지 못한다**. 3건 중 가장 약한 케이스다.

### 이벤트 C (ytn2 t=40.03 첫 오방출 `Thank`)

```
b109 t=35.23 lk=en  p_opp=0.0006   Output 'resolutions'   ← 정상 영어
b111 t=35.71 lk=en  p_opp=0.0001
b112 t=36.19 lk=en  p_opp=0.2212                           ← 상승 시작
b115 t=37.15 lk=en  p_opp=0.9997   ★ 첫 돌파
 …  b116~b123 전부 p_opp 0.9956~0.9998 (9배치 연속, 방출 0건)
b124 t=40.03 lk=en  p_opp=0.9991   Output 'Thank'          ← 실패 커밋
```

- **+2.88s 선제**. 9배치 연속으로 `p_opp≥0.99`가 유지되는 동안 **방출이 하나도 없다** —
  개입에 쓸 수 있는 순수 여유 시간이다. 3건 중 가장 강한 케이스다.

**선제성 판정: 3/3 이 "이전 또는 동시"** (A −0.80s / B 0.00s / C −2.88s). 사전 등록 기준 ② 충족.
단 B가 정확히 경계선(0.00s)이라는 점은 명시해 둔다.

## 4. `high_resid` (bong1 420/479 = 88%) 해석

**그 문턱은 무의미하다** — 그리고 대체 문턱도 없다.

```
전체 resid: n=479 min=0.4426 med=0.5133 max=0.9920 · resid>=0.5: 420/479 (88%)
표적 med 0.5243 / 대조 med 0.5120   ← 사실상 겹침
  resid>=0.5  표적 91.9%  대조 87.0%    (분리 없음)
  resid>=0.7  표적 18.9%  대조  5.1%    (표적 81%를 놓침)
  resid>=0.9  표적 13.5%  대조  1.0%    (표적 87%를 놓침)
```

`resid`는 배치의 88%에서 0.5를 넘으므로 그 문턱은 **상시 배경**이다. 더 높은 문턱은 대조를
줄이지만 **표적도 함께 잃어** 이벤트 검출률이 13~19%로 무너진다. 결정적으로 **ytn2에선 방향이
역전**된다(resid≥0.7: 표적 0/24 · 대조 7/201). **`resid`를 가르는 문턱은 존재하지 않는다.**
`H_lang`도 동일하게 ytn2에서 역전된다.

`resid`가 실제로 높아지는 곳은 무음·짧은 버퍼(seglen 0.24~0.96s) 구간이다 — 이벤트 A의
b29/b30(resid 0.985/0.992)이 그 예이며, 그 두 배치는 오히려 `p_opp`가 0.24~0.44로 **떨어지는**
지점이다. 즉 `resid`는 "언어 오류"가 아니라 "오디오 빈약"을 잰다.

## 5. 기전 — 프로브는 *버퍼의 내용*을 읽는다

선행 연구 §4는 lang_id 와 디코더가 **같은 `encoder_feature`를 공유**한다고 지적했다. 그 사실은
그대로다. 달라진 것은 **언제 읽느냐**다.

`p_opp`가 높다는 것은 "**지금 인코더에 들어있는 오디오의 언어**"와 "**강제된 언어 토큰**"이
어긋났다는 뜻이다. 관측된 4가지 거동이 이 한 가지 기전으로 전부 설명된다:

| 상황 | 버퍼 상태 | 프로브 | 정답 여부 |
|---|---|---|---|
| 이벤트 A | 리셋 직후, 신선한 한국어 0.24~0.96s | ko 0.9985 | **정확** (잠금이 틀림) |
| 이벤트 B | 전환 후 절단, 한국어 2.39s | ko 0.9832 | **정확** (잠금이 틀림) |
| 이벤트 C | 한국어 누적 중 | ko 0.9997 | **정확** (잠금이 틀림) |
| bong1 b246 (t=87.38) | 영어 **9.54s** 누적, 새 한국어는 아직 미유입 | en 0.9950 | **부정확**(지연) |
| bong1 b280 (t=98.43) | 영어 **5.52s** 누적 | en 0.9997 | **부정확**(지연) |

즉 프로브는 **버퍼에 대해선 항상 정확**하고, 버퍼가 낡았을 때만 새 오디오에 대해 **지연**된다.
이것이 §0의 확장 금지 범위의 근거다 — **전환 경계에서 프로브에 승인을 물으면 안 된다.**

## 6. 오탐 전수 분류 (59회 발동)

### bong1 R1 — 33건(계측 카운터 `mismatch_candidates=33`과 정확히 일치), 5개 버스트

| # | batch | t_abs | locked | n | p_opp med | 인접 방출 | 분류 |
|---|---|---|---|---|---|---|---|
| 1 | 22–28 | 14.34–16.26 | en | 6 | 0.9994 | `is the one`/`who is the main` | **참양성(이벤트 A)** |
| 2 | 31–34 | 16.98–17.46 | en | 4 | 0.9970 | `I think that's` | **참양성(이벤트 A)** |
| 3 | 96 | 38.82 | ko | 1 | 0.9538 | 직후 `son`/`who's holding` + LangSwitch ko→en | **정당(전환 선행감지)** |
| 4 | 319–323 | 112.23–113.67 | en | 5 | 0.9831 | `- Sorry`/`, my name` | **참양성(이벤트 B)** |
| 5 | 328–344 | 114.87–119.71 | en | 17 | 0.9940 | `metaphor`/`ical as`/`so...` | **참양성(이벤트 B)** |

**참양성 32 · 전환 선행감지 1 · 순수 오탐 0 · 판정불가 0**

### ytn2 R1 — 26건, 4개 버스트 (**Exp-160 스퓨리어스 재현 여부가 핵심**)

| # | batch | t_abs | locked | n | p_opp med | 인접 방출 | 분류 |
|---|---|---|---|---|---|---|---|
| 1 | 94–95 | 30.43–30.67 | ko | 2 | 0.9478 | 직후 `. In support of` + LangSwitch ko→en | **정당(전환 선행감지)** |
| 2 | 115–137 | 37.15–43.70 | en | 22 | 0.9993 | `Thank you very much…` (정답 문장 소실) | **참양성(이벤트 C)** |
| 3 | 229 | 74.33 | ko | 1 | 0.9873 | 직후 `. Overall,` + LangSwitch ko→en | **정당(전환 선행감지)** |
| 4 | 241 | 99.12 | ko | 1 | 0.9990 | 직후 `There is more work…` + LangSwitch ko→en | **정당(전환 선행감지)** |

**참양성 22 · 전환 선행감지 4 · 순수 오탐 0 · 판정불가 0**

> **Exp-160 재현 없음.** ytn2 발동 26건 중 22건은 실제 실패(C)이고, 나머지 4건은 전부
> **시스템이 어차피 수행한 정당한 ko→en 전환의 0.2~0.7s 선행 감지**다. 이 4건에서 재감지를
> 강제해도 결과는 같은 전환이므로 **스퓨리어스 전환을 새로 만들지 않는다**.
> **정상 전사가 안정적으로 진행 중인 구간에서 발동한 건은 두 파일 통틀어 0건이다.**

### 반대 방향 — 프로브가 놓친 곳(위양성 아닌 **위음성**)

`##2b` 분석에서 잠금 결정 28건 중 4건이 프로브와 배치됐다. 그중 **2건은 프로브가 틀린 쪽**이다:

| 지점 | 적용된 잠금 | 직전 프로브 | 실제 | 판정 |
|---|---|---|---|---|
| L552 (이벤트 A) | en | `p_en=0.0015` | 한국어였음 | 프로브 **정확** — veto 했어야 함 |
| L20535 (플라스틱 진입) | ko | `p_ko=0.0050` (buffer 9.54s 영어) | **한국어 맞음** | 프로브 **부정확** — veto 하면 파손 |
| L26485 | ko | `p_ko=0.0003` (buffer 5.52s 영어) | **한국어 맞음** | 프로브 **부정확** — veto 하면 파손 |
| L34973 (이벤트 B) | en | `p_en=0.4410` | 한국어였음 | 애매(0.44) — τ=0.9 규칙엔 안 걸림 |

**→ "전환을 프로브로 승인" 규칙은 이 데이터가 반증한다.** 정당한 한국어 전환 2건(플라스틱 포함)을
차단해 오히려 파손시켰을 것이다. 지지되는 용법은 **잠금 유지 중 지속 불일치 경보**뿐이다.

## 7. go/no-go 판정

사전 등록 기준에 대한 대조:

| 기준 | 결과 | 근거 |
|---|---|---|
| ① 표적/대조를 가르는 문턱 | **충족** | `p_opp` τ=0.97: bong1 표적 28/37 · 대조 **0/396**. 중앙값 0.991 vs 0.0001(3~4자릿수) |
| ② 검출이 이벤트 이전 또는 동시 | **충족(3/3)** | A −0.80s(잠금 적용 전) · B 0.00s · C **−2.88s**(방출 0건 여유) |
| ③ 그 문턱에서 ytn2 오탐 희소 | **충족** | 26건 중 22 참양성 + 4 정당전환선행. **순수 오탐 0건**, Exp-160 재현 없음 |

**판정: GO** — 단 아래 범위로 한정한다.

- **채택 대상 신호 = `p_opp` 단독.** `resid`·`H_lang`은 판별력 없음(ytn2에서 역전),
  `p_translate`·`p_nospeech`는 전 배치 0으로 **S3 반증**, `p_abs_*`는 `p_opp`의 거울상.
- **채택 대상 용법 = 지속 불일치 경보**(잠금 유지 중 `p_opp≥τ`가 N배치 연속 → 재감지 강제).
  단발 발동이 아니라 **연속성**을 요구해야 한다 — 이벤트 A의 b27/b29/b30처럼 무음·짧은 버퍼에서
  `p_opp`가 0.24~0.79로 순간 하락하는 구간이 있다(§4).
- **반증된 용법 = 전환 승인(switch-confirmation).** §6 후반 표대로 정당한 전환을 차단한다.
- 문턱은 **τ=0.97 부근**이 두 파일 공통으로 대조 발동을 0~1건까지 낮춘다.

낙관 편향을 경계해 덧붙인다: 이 GO는 효과크기가 **미세하지 않기 때문에** 내려졌다(대조 중앙값
0.0001 대 표적 0.99, 59회 발동 중 오탐 0). 만약 분리가 미세했다면 표본 2회차로는 NO-GO가 맞다.

## 8. 한계·미확인 사항

**반드시 감안할 것 — 이 판정을 뒤집을 수 있는 요인들이다.**

1. **표본이 bong1 R1 + ytn2 R1 각 1회차뿐이다.** 이벤트는 3건(A·B·C). 이 프로젝트는 실행마다
   편차가 크므로(§CLAUDE.md 4) 다른 회차에서 오탐이 나타날 가능성은 **배제되지 않았다**.
   Stage 1은 최소 `--repeat 3` 규모의 로그로 재확인해야 한다.
2. **방향 편중**: 관측된 3건 전부가 `locked=en` + 한국어 오디오다. **반대 방향(`locked=ko` +
   영어 오디오)의 실패는 이번 표본에 0건** — 그 방향의 판별력은 **미확인**이다.
3. **미측정 파일**: sbs1 · kor1~3 · eng1 · ytn1 · kinno 로그는 보지 않았다. 특히 **단일언어
   세션(`--lan ko`/`en`)에서의 오탐률은 완전히 미확인**이다. 한국어 단독 낭독(kor1~3)에서
   `p_opp`가 어떻게 거동하는지 모른다.
   → **§10에서 측정됨(kinno 제외).** 결과: 이 한계는 기우가 아니었다 — ko 세션에서 순수 오탐이
   실측돼 §7 판정의 근거 ③(“오탐 희소”)이 코드스위칭 파일 한정임이 드러났다.
4. **위음성률 미정량**: 두 파일의 모든 언어잠금 실패를 전수 열거하지 않았다. 확인한 것은
   "지목된 실패에 신호가 떴다"이지 "모든 실패에 신호가 뜬다"가 아니다. bong1 R1의
   언어불일치율 20.9%가 A·B로 전부 설명되는지는 **미확인**.
5. **이벤트 B의 선제성은 정확히 0.00s**(동시)다. 사전 등록 기준상 충족이지만, 첫 오방출 자체는
   막지 못한다. 세 이벤트 중 하나가 경계선이라는 점은 GO의 강도를 낮춘다.
6. **개입 효과는 전혀 측정되지 않았다.** 이 분석은 *검출 가능성*만 보였다. 재감지를 강제했을 때
   WER/화자분리 F1이 실제로 개선되는지는 별개 문제이며, 재감지 자체가 실패할 수도 있다
   (b246/b280에서 보듯 버퍼가 낡았으면 재감지도 틀린 답을 낼 수 있다).
7. **표적 창을 분석자가 지정했다**(순환 위험). 다만 §6의 mismatch 전수 열거는 창과 무관하게
   수행되며 같은 결론에 도달한다 — 발동 59건이 전부 3개 이벤트 + 5개 실제 전환 경계에
   떨어진다. 이벤트 C가 창 지정 없이 발굴된 것도 독립 증거다.

## 9. 다음 단계 제안 (Stage 1)

1. **위음성·반대방향 닫기**: 같은 계측으로 `--lan ko`(kor1~3) · `--lan en`(eng1) · sbs1 로그를
   수집해 ①단일언어 세션 오탐률 ②`locked=ko` 방향 판별력을 측정. **한계 3·2를 직접 겨냥.**
2. **연속성 파라미터 결정**: `p_opp≥τ`가 몇 배치 연속이어야 무음 구간 순간하락(§7 한계 5)에
   걸리지 않는지 — 현재 데이터로도 A(6/4배치) · B(5/17배치) · C(9배치)와 정당전환(1~2배치)이
   **버스트 길이에서도 분리**되는 것으로 보인다. 이 축을 정량화하면 오탐 여유가 더 커진다.
3. **그 다음에야 개입 구현**. 검출과 개입은 별개이며, 개입 효과는 경로 C 측정으로만 판정한다.

---

## 10. 표본 확대 — 단일언어 세션 오탐률 (Exp-195, 2026-07-21)

§8 한계 3(“단일언어 세션 오탐률 완전 미확인”)과 §9 제안 1을 직접 겨냥해 측정했다.
**코드 변경 0** — §1~§9와 동일한 계측(`exp/lang-lock-stage0@5466b87`)으로 파일만 늘렸다.

측정: 경로 C · `--repeat 1`(스크리닝) · 화자분할 ON · CRT 3.0 · `--trace-tokens` · `vbcable=ok`.
언어모드별로 run 분리(`--lan ko` / `--lan en` / `--lan auto`).

### 10-1. 한 줄 결론

**§8 한계 3의 답은 “오탐률 0이 아니다”** — ko 단일언어 세션에서 `p_opp ≥ 0.97` 발동이 **5건**
관측됐고 그중 최소 2건은 **정상 전사 구간의 순수 오탐**이다. §6이 bong1·ytn2에서 얻은
“순수 오탐 0건”은 **코드스위칭 파일에 한정된 결과**였다.

다만 발동 5건은 **전부 단발(K=1)이고 버퍼가 0.24~1.02s**로, §9 제안 2가 요구한 연속성 조건과
인계서 §4 게이트의 `segments_len() ≥ 2.0s` 조건 **양쪽 모두에서 걸린다**(오프라인 재현 기준
would_fire 0). 즉 **신호 층위에서는 전제가 깨졌고, 게이트 층위에서는 유지된다.**

이 두 문장은 이후 `--repeat 3` 확증 측정(§10-6)에서 **ko 12세션 3756 프로브 규모로 재확인**됐다 —
발동 23건(0.61%, 재현성 있음) / `seglen≥2.0s` 발동 **0건** / 게이트 통과 **0/23**.

### 10-2. 정량 (파일별)

| 파일 | 모드 | WER | 화자F1 | 문장F1 | LMR_ko | probes | 발동 τ=0.9¹ | 발동 τ=0.97 | 연속 run | 게이트 통과² |
|---|---|---|---|---|---|---|---|---|---|---|
| kor1 | ko | 40.9% | 0.0%³ | 75.0% | 0.0% | 365 | 5 | **3** | 3 (전부 K=1) | **0** |
| kor2 | ko | 18.6% | 100.0%³ | 100.0% | 0.0% | 285 | 3 | **1** | 1 (K=1) | **0** |
| kor3 | ko | 33.8% | 100.0%³ | 100.0% | 0.0% | 321 | 2 | **1** | 1 (K=1) | **0** |
| eng1 | en | 5.7% | 0.0%³ | 66.7% | n/a⁴ | 121 | 0 | **0** | 0 | **0** |
| sbs1 | auto | 11.3% | 100.0% | 94.7% | 0.0% | 249 | 1 | **0** | 0 | **0** |
| ytn1 | auto | 20.2% | 82.4% | 40.0% | **19.7%**(9.2%p) | 266 | 32 | **25** | 4 | **2** |

¹ 계측 코드 카운터 `mismatch_candidates`(상수 `SOT_PROBE_MISMATCH_P=0.9`). Stage 1 설계 문턱은 τ=0.97이라 값이 다르다 — 혼동 주의.
² 오프라인 재현: `K≥3 연속배치 ∧ 지속 T≥1.0s ∧ 그 안에 seglen≥2.0s 3연속`. 런타임 게이트(쿨다운·타 arm 중복방지)는 **미반영**이므로 상한이다.
³ 단일화자 낭독의 화자F1 0/100 극단은 산식 경계 아티팩트(인계서 §7). 실제 화자분리 실패 아님.
⁴ eng1은 정답에 KO 단어가 0개(`ko_ref_words=0`)라 `lmr_ko` 미정의. 역방향 `lmr_en=0.0`, `en_to_ko=0`.

**LMR 교차검증**: 단일언어 세션 4종 전부 `ko_to_en=0` · `en_to_ko=0`(kor1의 `lmr_en=1.0`은
`en_ref_words=1`인 축퇴 케이스로 WER 귀속 0.6%p) — §2-1이 예상한 “단일언어 세션 LMR≈0”과 일치하며,
**LMR과 프로브 발동이 서로 독립적으로 같은 결론**(단일언어 세션엔 언어잠금 실패가 없다)을 낸다.
따라서 ko 발동 5건은 “LMR이 놓친 실패”가 아니라 신호 쪽 오탐이다.

### 10-3. ko 세션 발동 전수 분류 (5건)

| # | 파일 | batch | t_abs | seglen | p_opp | 구간 전사 | 분류 |
|---|---|---|---|---|---|---|---|
| 1 | kor1 | 1 | 1.02s | 1.02s | 0.9961 | `자막 제공 / 및 자막 / 제공 배달의민족` | **환각 구간 동시발생** — 정답 “전투발전부장 최창수 육군 소장입니다”가 통째 유실되고 전형적 한국어 자막 환각으로 대체된 자리다. 정상 전사 구간이 아니다 |
| 2 | kor1 | 20 | 6.60s | 0.24s | 0.9859 | 직후 `발 / 표하겠습니다 / .` | 회색 — 방출 자체는 정상이나 서두 붕괴(#1)의 여파 구간 |
| 3 | kor1 | 26 | 10.82s | 0.24s | 0.9921 | 직후 `스선을 보시 / 는 / 바와 같습니다` | 회색 — 앞 음절 결손(`스선`) |
| 4 | kor2 | 1 | 3.99s | 0.24s | 0.9921 | 직후 `먼저 / 육군` (정답 “먼저 육군은…”) | **순수 오탐** |
| 5 | kor3 | 30 | 12.95s | 0.24s | 0.9969 | 직후 `원 / 거리 / 타격` | **순수 오탐** |

→ **순수 오탐 2건 확정, 회색 2건, 환각동시 1건.** §6의 “순수 오탐 0”은 유지되지 않는다.

**공통 구조 — 전부 극소 버퍼의 단발이고 즉시 자기교정된다:**

- 5건 중 4건이 `seglen=0.24s`. kor2 #4는 직전에 `Refreshing segment` / `[RefreshSegment] complete=T`가
  찍혀 있어 **refresh 직후 잔여 버퍼**임이 로그로 확인된다 — 인계서 §4가 `seglen ≥ 2.0s`를
  “가장 중요”하다고 지목하며 막으려던 Exp-189 실패모드 그 자체다.
- **다음 배치에서 곧바로 문턱 아래로 떨어진다**: #4는 b2(seglen 0.72s)에서 `p_ko=0.981`로 완전 반전,
  #1은 b2에서 `p_en=0.128`, #2는 `0.198`, #3은 `0.678`, #5는 `0.590`. K=3 연속은 한 번도 성립하지 않는다.
- 발동 시각이 전부 세션 도입 13초 이내다(1.02·3.99·6.60·10.82·12.95s).

### 10-4. auto held-out 결과

- **sbs1**: τ=0.97 발동 **0건**(τ=0.9에서 1건). `max_resid=0.6527`로 6파일 중 유일하게 낮다.
- **ytn1**: 25건 / 4 run, 전부 `locked=en`. 정답은 한국어(`안녕하십니까 / 지난 세 달 만에 두 번째로
  대한민국에 다시 방문할 수 있게 되어 기쁘게 생각합니다`)인데 전사는
  `Yeah, I'm not sure how many years I'm happy to be here. to be able to visit the United States` —
  **의미 보존형 영어 번역**, §1이 정의한 언어잠금 실패의 교과서적 형태다. LMR_ko 19.7%(9.2%p),
  `ko_to_en=15`가 이를 독립 확증한다. → **25건 전부 참양성**, Exp-160 스퓨리어스 재현 없음.

  | run | batch | t_abs | K | 지속 | seglen | 게이트 |
  |---|---|---|---|---|---|---|
  | 1 | 9–12 | 4.18–5.14s | 4 | 0.96s | 4.18–5.14s | BLOCK (T<1.0s, 경계) |
  | 2 | 17 | 6.34s | 1 | 0.00s | 6.34s | BLOCK |
  | 3 | 19–24 | 7.06–8.26s | 6 | 1.20s | 7.06–8.26s | **PASS** |
  | 4 | 26–39 | 8.98–12.91s | 14 | 3.93s | 0.72–4.64s | **PASS** |

  §8 한계 2(반대방향 미확인)는 **여전히 미해소** — ytn1도 `locked=en` 방향이다. 6파일을 통틀어
  `locked=ko` + 영어 오디오 실패는 0건이라 그 방향의 판별력은 이번에도 측정되지 않았다.

### 10-5. 판정 — Stage 1 진행 보류 (사용자 판단 대기)

이번 측정의 사전 등록 분기 기준은 “ko/en 단일언어 세션에서 **정상 전사 구간 발동 0건**, 또는 발동이
전부 정당전환 선행으로 설명됨”이었다. 실측은 **둘 다 아니다**(순수 오탐 2건, kor1~3은 설계상 단일언어
낭독이라 정당전환이 존재할 수 없다). 따라서 사전 약속대로 **Stage 1 섀도우 구현에 착수하지 않고 정지**한다.

사용자 판단을 위해 양쪽 근거를 같이 둔다.

- **계속해도 된다는 근거**: 발동 5건이 전부 K=1·극소버퍼이고, 인계서 §4 게이트는 이 실패모드를
  **사후 튜닝 없이 이미** 배제하도록 설계돼 있었다(`seglen≥2.0s`가 “가장 중요”). 오프라인 재현에서
  would_fire 0. 반대로 ytn1의 참양성 run 2개는 게이트를 통과한다 — 신호 대 잡음이 게이트 뒤에서
  깨끗하게 갈린다.
- **멈춰야 한다는 근거**: Exp-189가 경고한 “p=0.95~1.00 고신뢰 오탐”이 **실제로 재현됐다**.
  게이트가 막아준다는 판단은 `--repeat 1` 표본 1회에 의존하며, `seglen≥2.0s`를 넘는 오탐이
  다른 회차에 없다는 보장은 없다. 이 프로젝트는 stdev 6.1%p가 5/10 빈도 현상을 가린 전례가 있다(인계서 §6-7).

> **§10-6 추가 측정 후 갱신**: 위 “멈춰야 한다” 근거 중 *표본 1회 의존* 부분은 해소됐다 —
> ko 12세션 3756 프로브에서 `seglen≥2.0s` 발동은 **0건**, 게이트 통과 **0/23**이다.
> 남은 위험은 “재현되는 고신뢰 오탐이 존재한다”는 사실 자체이며, 판정(정지)은 그대로 유지한다.

### 10-6. ko 세션 `--repeat 3` 확증 측정

§10-5의 판정이 스크리닝 1회에 얹혀 있다는 약점을 메우기 위해, kor1~3을 `--lan ko --repeat 3`으로
재측정했다(코드 변경 0, 동일 설정, `vbcable=ok`). **9개 세션 추가 → ko 세션 누계 12개.**

WER median(N=3): kor1 **21.6%**(min 16.4 / max 26.3 / stdev 5.0) · kor2 **24.1%**(19.3 / 24.8 / 3.0) ·
kor3 **35.8%**(28.5 / 39.1 / 5.4). **LMR은 9/9 세션 전부 0.0%**(`ko_to_en=0`).

| 세션 | probes | 발동 τ=0.97 | run | 게이트 통과 | 발동 상세 |
|---|---|---|---|---|---|
| kor1 R1 | 331 | 2 | 2 | 0 | b26(K=1, seg 0.48) · b32(K=1, seg 0.24) |
| kor1 R2 | 328 | **0** | 0 | 0 | — |
| kor1 R3 | 352 | 2 | 1 | 0 | b24(K=2, dur 0.24s, seg 0.24–0.48) |
| kor2 R1 | 292 | 1 | 1 | 0 | b1(K=1, seg 0.24) |
| kor2 R2 | 286 | 3 | 3 | 0 | b1 · b153 · b193 (전부 K=1, seg 0.24) |
| kor2 R3 | 286 | 2 | 2 | 0 | b1 · b193 (K=1, seg 0.24) |
| kor3 R1 | 313 | 3 | 3 | 0 | b1 · b30 · b96 (K=1, seg 0.24) |
| kor3 R2 | 322 | 3 | 3 | 0 | b1 · b30 · b114 (K=1, seg 0.24) |
| kor3 R3 | 272 | 2 | 2 | 0 | b1 · b30 (K=1, seg 0.24) |
| **N=3 합계** | **2782** | **18 (0.65%)** | 17 | **0** | 최대 K=2 |

**ko 세션 누계(N=1 + N=3, 12세션 3756 프로브)**

| | 값 |
|---|---|
| 발동 τ=0.97 | **23건 (0.61%)** |
| 발동 시 `seglen` 분포 | 0.24s ×20 · 0.48s ×2 · 1.02s ×1 — **최대 1.02s** |
| `seglen ≥ 2.0s` 발동 | **0건** |
| 최대 연속 K | **2** (K=3 미달) |
| 게이트 통과 | **0 / 23** |
| 세션 초입 15초 이내 발동 | 17 / 23 |

**해석 — 두 결론이 동시에 굳어졌다.**

1. **오탐은 실재하고 재현된다.** 0.61%는 1회 표본의 우연이 아니다. 오히려 **결정론적**이다 —
   kor2 b1은 3/3 회차, kor3 b1·b30은 3/3 회차에서 같은 지점에 반복 발동한다. §10-3의
   “정상 전사 구간 순수 오탐”은 확정 사실이 됐고, §6의 “순수 오탐 0”은 최종적으로 뒤집혔다.
2. **그러나 오탐은 하나의 좁은 체제(regime)에만 산다.** 23건 전부 `seglen ≤ 1.02s`이고
   K가 3에 도달한 적이 없다. `segments_len() ≥ 2.0s` 조건 **하나만으로 23/23이 배제**된다.
   이는 사후에 맞춘 문턱이 아니라 인계서 §4가 Exp-189 대응으로 **미리** 지정해 둔 값이다.

즉 “신호 층위에서 전제가 깨졌다”와 “게이트 층위에서 전제가 유지된다”가 둘 다 12세션 규모로 확인됐다.

### 10-7. en 방향 확증 + 오탐/참양성의 **방향 분리**

eng1도 `--lan en --repeat 3`으로 재측정해 en 쪽 표본을 닫았다(코드 변경 0).
WER median **3.8%**(min 2.9 / max 6.7 / stdev 2.0).

| | eng1 누계 |
|---|---|
| 세션 | **4** (N=1 run 1 + N=3 run 3) |
| probes | 500 |
| 발동 τ=0.97 | **0건** |

**이로써 안 A의 사전 등록 대상 표본이 전부 닫혔다.** 전체 그림:

| 방향 | 세션 | probes | 발동 | 성격 | 게이트 통과 |
|---|---|---|---|---|---|
| `locked=ko` (ko 세션) | 12 | 3756 | **23 (0.61%)** | **전부 오탐/회색** — 참양성 0 | **0 / 23** |
| `locked=en` (en 세션) | 4 | 500 | **0** | — | 0 |
| `locked=en` (auto, ytn1) | 1 | 266 | 25 | **전부 참양성** | 2 run |
| auto, sbs1 | 1 | 249 | 0 | — | 0 |

**관측: 오탐과 참양성이 방향으로 깨끗이 갈린다.**

- 순수 오탐 23건은 **전부 `locked=ko`** — 즉 `p_opp = p_en`이 튄 경우다.
- 참양성(bong1 A·B, ytn2 C, ytn1 2버스트)은 **전부 `locked=en`** — `p_opp = p_ko`가 튄 경우다.
- 15세션을 통틀어 `locked=ko` 참양성은 **0건**, `locked=en` 오탐도 **0건**이다.

**해석(가설, 미검증)**: 극소 버퍼(0.24~1.02s)의 정보량이 부족할 때 사후분포가 Whisper의
지배적 사전확률인 영어 쪽으로 끌린다면, `locked=ko`에서만 `p_opp`가 튀고 `locked=en`에서는
튈 이유가 없다 — 관측된 완전한 방향 분리와 일치한다. 다만 이는 **데이터와 정합적인 설명일 뿐
검증된 기전이 아니다**(사전확률을 직접 측정하지 않았다).

**설계 함의**: 사실이라면 방향 비대칭 문턱(`locked=ko`일 때만 τ를 올리거나 최소 버퍼를 더 요구)이
자연스러운 레버가 된다. 단 이는 **Stage 1 설계 영역이므로 이번 세션에서 구현하지 않는다** —
게이트 파라미터를 실측에 맞춰 사후 조정하는 것은 §10-5가 정지 사유로 든 바로 그 행위다.
기록만 남기고 판단은 사용자에게 넘긴다. 또한 `locked=ko` 방향의 **참양성 판별력은 여전히 미측정**이라
(그 방향 실패 사례가 0건) 비대칭 문턱이 진짜 실패를 놓치는지는 알 수 없다.

### 10-8. 이번 측정이 남긴 한계

- ko 오탐률 0.61%는 이제 방향 신호가 아니라 **12세션 기반 추정치**다. 다만 `--repeat 3`은
  이 프로젝트가 채택 확정에 쓰는 최소 규모이지 대표본은 아니다.
- ~~en 방향은 여전히 N=1~~ → **§10-7에서 N=3까지 확증**(4세션 500프로브, 발동 0).
- 게이트 통과 판정은 **로그 사후 재현**이지 런타임 게이트 실행이 아니다. 쿨다운·타 트리거 arm
  중복방지는 모델링하지 않았다(그 조건들은 발동을 **더** 줄이므로 표의 통과 수는 상한이다).
- **반대 방향(`locked=ko` + 영어 오디오) 참양성 판별력은 미측정**이다 — 15세션 통틀어 그 방향 실패 0건.
  §10-7의 방향 분리 관측이 “`locked=ko` 발동은 전부 오탐”을 뜻하는지, 아니면 단지 그 방향 실패가
  이 데이터셋에 없었을 뿐인지는 **구분되지 않는다**.
- 위음성률 미정량 · 개입 효과 전혀 미측정 · kinno 미측정
  (bong1/ytn2는 §1~§9 로그 그대로 유효, 재측정 없음).

---

### 재현

```
python scripts/analyze_sot_lang_probe.py \
    --log .omc/server_logs/server_bong1_C_R1_20260721_212212.log \
    --target "A:14.3-17.5" --target "B:112.2-119.3" \
    --control-window "plastic:88.0-93.0"

python scripts/analyze_sot_lang_probe.py \
    --log .omc/server_logs/server_ytn2_C_R1_20260721_212532.log \
    --target "C:37.0-44.0" --lead 2.0

python scripts/analyze_sot_lang_probe.py --log <로그> --list-mismatch   # 오탐 스캔만
```

`.omc/`는 저장소 밖(미추적)이라 워크트리에 따라 없을 수 있다 — 없으면 로그 절대경로를 넘길 것.
스크립트는 표준 라이브러리 전용이며 로그를 수정하지 않는다. 로그 마커 정규식은
`scripts/analyze_translit_langid.py`(브랜치 `exp/codeswitch-translit-probe`)의 검증된 패턴을
그대로 쓰되, 그 파일이 이 브랜치 워크트리에 없어 import 대신 정의를 복제했다 —
로그 포맷이 바뀌면 **양쪽을 함께** 고쳐야 한다.
